REGISTRO DE CAMBIOS - Catalogo Almacen
Formato: [inicio] | descripcion | agente | version | [fin]   (horas de Colombia, UTC-5)
Versionado: mayor = cambio estructural | menor = modulo nuevo | parche = ajuste minimo
Version de partida antes de este registro: V 1.0.0 (catalogo publico, carrito, solicitudes y panel).

2026-09-23 16:05 | UI: version en la URL de app.css y app.js (?v=filemtime) en los dos layouts y en admin/login. En produccion (IIS) el navegador seguia recibiendo el app.css y el app.js anteriores despues del despliegue, asi que ni las flechas de orden ni el filtro automatico de Solicitudes se veian. Con la fecha de modificacion en la URL, cada archivo nuevo es una URL nueva y ninguna cache sirve la copia vieja. | claude | V 1.4.7 | 2026-09-23 16:10

2026-09-23 15:10 | SOL-02 (ajuste): los cuadros de filtro por columna de la pantalla Solicitudes filtran solos, sin apretar el boton. Nuevo atributo data-autofiltrar en app.js: envia el formulario 600 ms despues de la ultima tecla (Enter envia en el acto), no recarga si el valor no cambio y, como el envio recarga la pagina, guarda en sessionStorage el cuadro con foco y la posicion del cursor para devolverlos al cargar (envuelto en try/catch: sin almacenamiento solo se pierde el foco). El boton de filtrar se deja como respaldo sin JavaScript. Pruebas de las bandejas en verde; sin revision visual en navegador. | claude | V 1.4.7 | 2026-09-23 15:15

2026-09-23 12:41 | SOL-02: pantalla Solicitudes del panel (las dos bandejas, admin.solicitudes.index y admin.solicitudes.listos). SE RETIRO EL BUSCADOR GENERAL (parametro q) y en su lugar va una fila de filtros bajo los encabezados para Numero, Solicitante, Items, Estado y Atendida por, mas flechas de orden asc/desc en cada uno de esos titulos; Recibida y Accion no llevan ni filtro ni orden. Filtros en el servidor en el nuevo Solicitud::scopeFiltrarPorColumnas(): Numero pasa por normalizarNumero() (igualdad exacta con 000004, 4 o SOL-2026-000004) y solo cae a LIKE parcial cuando no es un numero; Solicitante busca en nombre, cedula y solicitante_area; Items es igualdad sobre el conteo (has) y un valor no numerico se ignora en vez de volverse 0; Atendida por busca en users.name; todos los LIKE con el escape de corchetes de SQL Server. ESTADO ES UN SELECT y no un cuadro de texto: son cinco valores cerrados y usa el MISMO parametro estado que las tarjetas de conteo, asi que tarjeta y filtro no se pueden contradecir; el controlador solo acepta claves de Solicitud::ESTADOS (lo demas equivale a todos). En Listos el estado lo sigue fijando la ruta: el select sale deshabilitado y sin name y un estado por URL se ignora. Orden en Solicitud::scopeOrdenarBandeja() con LISTA BLANCA (Solicitud::COLUMNAS_BANDEJA) resuelta con un match, sin interpolar el request: Items por subconsulta count(*), Atendida por subconsulta al nombre del usuario, Estado por avance del flujo y no alfabetico, Numero por la columna; desempate created_at desc, id desc; un orden invalido cae al orden de siempre sin error. bandeja() depura los parametros una sola vez y la paginacion pasa de withQueryString() a appends() sobre esa consulta depurada; enlaces de orden, tarjetas y quitar filtros conservan filtros y orden. La tabla se pinta aunque no haya resultados para no perder la fila de filtros. Accesibilidad: labels ocultos, aria-sort y aria-label en los enlaces de orden; en movil la tabla se desplaza dentro de table-responsive gracias a anchos minimos en app.css. Solicitud::scopeBuscar() SE ELIMINO: no le quedaba consumidor y su caso especial quedo cubierto por el filtro Numero. Pruebas: nueva SolicitudesFiltrosOrdenTest (18 casos) y ajustes en SolicitudesListosTest y SolicitudNumeroTest; suite completa 196 pruebas en verde y pint limpio. Sin revision visual en navegador. | developer | V 1.4.7 | 2026-09-23 14:55

2026-09-23 10:25 | SOL-01: el consecutivo de la solicitud pasa de SOL-{anio}-{6 digitos} a solo los seis digitos (000001, 000002...). SE CAMBIO EL DATO GUARDADO, NO LO QUE SE MUESTRA: la migracion 2026_09_23_100000_quitar_prefijo_anio_a_solicitudes_numero le quita el prefijo a las filas que ya existian (4 en desarrollo, y sirve para cualquier cantidad y cualquier anio del prefijo, no solo 2026), es idempotente porque el UPDATE se acota a las filas que todavia lo traen, y ANTES DE ESCRIBIR UNA SOLA FILA aborta con el detalle si dos anios distintos colisionaran en el mismo consecutivo (SOL-2025-000001 y SOL-2026-000001 quedarian con el mismo numero y la columna tiene indice unico); el down() repone el prefijo reconstruyendo el anio desde created_at, que es el que usaba el generador anterior, y el ida y vuelta se probo contra la base real. EL CONTADOR ES GLOBAL Y NO SE REINICIA POR ANIO, que es la consecuencia que no se puede perder: sin el anio adentro del numero, volver a 000001 cada enero chocaria contra el indice unico, asi que SolicitudService::siguienteNumero() toma MAX(numero) de toda la tabla en vez del maximo del anio. La columna es texto y el maximo se calcula a proposito con un filtro like '[0-9][0-9][0-9][0-9][0-9][0-9]': con seis digitos rellenados con ceros a la izquierda el orden lexicografico coincide con el numerico (000009 < 000010), pero cualquier valor que no sean seis digitos (un numero historico sin migrar, o los numeros de las pruebas) seria MAYOR comparando texto y al pasarlo por (int) daria 0, o sea que el contador volveria a 000001 y chocaria para siempre contra el indice unico; hay prueba que lo sostiene. Pasado 999999 el servicio falla con RuntimeException y mensaje explicito en vez de generar un numero de siete digitos que se saldria del filtro. SE SIGUE ACEPTANDO EL FORMATO VIEJO EN LA ENTRADA, y la decision es deliberada: los correos ya enviados y los numeros que la gente anoto dicen SOL-2026-000004, y romperlos seria dejar sin consultar su pedido a quien no hizo nada mal. La traduccion vive en UN SOLO SITIO, Solicitud::normalizarNumero(), que acepta 000004, 4 y SOL-{cualquier anio}-000004 y devuelve null cuando el texto no es un numero de solicitud, para que quien llama distinga "esto no aplica" de un numero valido sin volver a mirar el texto; la usan SolicitudController::consultar() (si devuelve null no se consulta la base) y Solicitud::scopeBuscar(), donde se agrega como IGUALDAD EXACTA y no como LIKE para no ensuciar el resultado cuando el termino es en realidad una cedula corta. El escape de LIKE con corchetes del buscador queda intacto. Puntos de consumo revisados uno por uno y ajustados: vista publica de consulta (el placeholder decia SOL-2026-000001), asuntos y cuerpos de los dos correos, que ahora dicen "No. 000004" porque seis digitos sueltos en un asunto no se leen como un numero de pedido, listado y detalle del panel, confirmacion y el partial de detalle (imprimen el atributo y no necesitaron cambio), la ruta /confirmacion/{numero} con su flash de sesion (el flash guarda el numero nuevo; un enlace viejo cae en /consultar, que ya normaliza) y StoreSolicitudRequest, que no valida el numero porque nunca lo recibe. La columna sigue en varchar(30) A PROPOSITO: estrecharla en SQL Server obliga a soltar y rehacer el indice unico, y no se gana nada. Pruebas: nuevo tests/Feature/SolicitudNumeroTest.php con 7 casos (formato de seis digitos sin prefijo; continuidad del contador sembrando un salto deliberado, para que no pase por bueno un contador que arranque en 1 ni uno que cuente filas; un numero historico que no se lleva el maximo; la normalizacion y sus null; la consulta publica con el formato viejo; que la consulta publica sigue exigiendo la cedula; y el buscador del panel con el formato viejo). Las dos fixtures que ya existian (SolicitudesListosTest y PermisoAdminTest) se dejaron con su marca no numerica SOL-TEST/SOL-PRUEBA y se comento por que: asi no entran en el MAX() del consecutivo. Suite completa 178 en verde y pint limpio. | developer | V 1.4.6 | 2026-09-23 10:52

2026-09-21 15:05 | ADM-05: el modulo Funciones por perfil (/admin/permisos) quedo RESERVADO a los perfiles administradores. Nuevo mecanismo generico en App\Models\Funcionalidad: la constante ROLES_ADMINISTRADORES es la lista blanca de claves de rol autorizadas y RESERVADAS el mapa clave de funcionalidad => lista blanca, hoy con una sola entrada (permisos). Funcionalidad::rolAutorizado() es su unico lector y User::puede() lo consulta ANTES QUE TODO LO DEMAS, incluido el rol del sistema admin: una lista blanca que se evalua despues de la matriz no es una lista blanca. La restriccion vive en el unico punto de decision, asi que el middleware permiso:permisos,accion, el authorize() de PermisoRolRequest, la directiva @puede, el Gate permiso y el menu lateral la heredan solos, sin una sola condicion nueva en controladores ni vistas. MAPEO DE PERFILES, QUE ES LO QUE HAY QUE RECORDAR: el usuario pidio superadministrador y administrador, pero en tbl_rol solo existen admin (col_nombre Administrador, rol del sistema) y almacenista, asi que el perfil administrador de esta instalacion ES admin; la lista lo incluye junto con superadministrador y administrador, previstas para el dia que alguien cree un perfil con esos nombres desde /admin/roles. No se creo ni se renombro ningun rol: la lista es una constante, no filas en la base, y ampliarla es agregar una clave en un solo sitio. La clave que se juzga es la del rol vigente (nuevo User::claveDeRolVigente(): el rol asignado mientras este activo, y si no el texto historico users.rol), el mismo criterio con el que puede() decide todo lo demas, para que la lista blanca y la matriz nunca juzguen roles distintos. DEFENSA EN PROFUNDIDAD SIN DUPLICAR LA DECISION: PermisoService::guardar() escribe la funcionalidad reservada en cero para el rol no autorizado (normalizar() recibe un veto que gana sobre el  del admin del sistema), FuncionalidadSeeder tampoco la siembra en true, y PermisoService::matriz() devuelve reservada => true por funcionalidad para que la pantalla pinte esas casillas bloqueadas y desmarcadas con una nota que explica que marcarlas no daria acceso. Nada de eso es la barrera: la barrera es puede(). Se guarda en cero a proposito para que, si algun dia alguien quita la lista blanca, no reviva sola una concesion que nunca estuvo vigente. CONSECUENCIA DELIBERADA: permisos ya no se hereda por col_gestiona_catalogo a un rol fuera de la lista, aunque siga figurando en HEREDADAS_DE_GESTIONA_CATALOGO (esa constante describe lo que hacia es.admin, no lo que se concede hoy); tres pruebas existentes cambiaron de resultado por diseno y se actualizaron diciendo por que. Sin migracion y sin cambios de esquema. Cinco pruebas nuevas en PermisoAdminTest (perfil no autorizado con la matriz marcada a mano en la base recibe 403 en GET y en PUT y no ve la entrada del menu; perfil de la lista blanca entra y guarda; las demas funcionalidades siguen mandandose por matriz; la pantalla bloquea solo las casillas reservadas; el admin del sistema no las ve bloqueadas): 26 en el archivo y 171 en toda la suite, todas en verde, y pint limpio. | developer | V 1.4.5 | 2026-09-21 15:48

2026-09-21 14:20 | CAT-14: nueva columna repuestos.fecha_actual_ERP (datetime nullable) que registra cuando el ERP confirmo por ultima vez el repuesto, y la sincronizacion de inventario DEJO DE MOVER fecha_actualizacion. EL PROBLEMA ERA QUE UNA SOLA COLUMNA RESPONDIA DOS PREGUNTAS DISTINTAS, O SEA QUE NO RESPONDIA NINGUNA: fecha_actualizacion es el UPDATED_AT del modelo (App\Models\Repuesto declara const UPDATED_AT = 'fecha_actualizacion'), asi que el Repuesto::whereIn(...)->update(['stock' => ...]) de SincronizadorStockRepuestos::escribirStock() se la movia a miles de filas en cada corrida; con eso era imposible saber si una fila la habia editado una persona en el panel o solo la habia rozado el ERP, ni hace cuanto que el ERP dejo de reportar un item. Ahora las dos escrituras del sincronizador van dentro de Repuesto::withoutTimestamps(), que es la forma expresiva de decir "esta sentencia no es una edicion", y la marca de la corrida vive en su propia columna. LA FECHA SE ESCRIBE A TODOS LOS REPUESTOS QUE EL ERP CONFIRMO, NO SOLO A LOS QUE CAMBIARON DE STOCK, y esa es la decision de fondo: si solo se marcaran los que cambian, un repuesto con existencia estable durante meses —que es lo normal en media bodega— apareceria con la fecha de hace meses y se leeria como que el ERP dejo de reportarlo, justo lo contrario de lo que pasa; marcando a todos los confirmados, una fecha vieja significa de verdad que el item se cayo del inventario del ERP, que es la senal util. Para poder marcarlos, cruzarContraElCatalogo() devuelve ahora los CODIGOS de los que ya estaban al dia y no solo cuantos son; los que si cambian reciben stock y fecha_actual_ERP en la MISMA sentencia (misma fila, mismo lote, no cuesta una pasada mas) y los que no cambian reciben una sentencia aparte que escribe unicamente la fecha, los dos caminos en lotes de 120 codigos por el limite de 2100 parametros de SQL Server. Toda la corrida comparte UNA sola marca de tiempo, calculada una vez antes de escribir, para que las miles de filas de una misma corrida queden agrupables: con un now() por sentencia, una corrida de varios minutos dejaria un abanico de fechas que no distingue una corrida de otra. El contador "actualizados" sigue significando "repuestos cuyo stock CAMBIO de valor" y no "filas escritas", que es la distincion que costo encontrar en agosto y que sostienen las pruebas; escribir la fecha no la toco. En --simular no se escribe nada, tampoco la fecha: una simulacion no deja rastro en ninguna parte. La columna es NULLABLE a proposito y las 28.490 filas existentes quedaron en NULL: nunca han recibido confirmacion del ERP y NULL dice exactamente eso, mientras que rellenarlas con la fecha de la migracion o con fecha_actualizacion seria inventar una confirmacion que no ocurrio. El nombre conserva ERP en mayusculas por peticion explicita del usuario aunque el resto de la tabla vaya en minusculas: en SQL Server el identificador no distingue la caja, pero Eloquent SI la distingue al declarar el cast y al leer el atributo, asi que el nombre exacto quedo documentado en la migracion y en el modelo. fecha_actual_ERP NO es fillable: la escribe unicamente la sincronizacion y jamas un formulario. Migracion aditiva e idempotente (no hace nada si la columna existe, y down() la quita solo si existe), sin ->after() porque SQL Server no lo admite. 6 pruebas nuevas: cuatro en SincronizarStockTest (escribe la fecha en los que cambiaron, la escribe tambien en los que ya estaban al dia, NO mueve fecha_actualizacion en ninguno de los dos caminos, y --simular no la escribe) y dos en RepuestoFotoAdminTest que sostienen el otro lado —editar el nombre y ajustar la existencia desde el panel SI mueven fecha_actualizacion—, porque si esa columna dejara de moverse nadie se enteraria: no hay pantalla que la muestre. 166 pruebas en verde y pint limpio. | developer | V 1.4.4 | 2026-09-21 14:50

2026-09-21 10:52 | ADM-05: el boton "Actualizar" del inventario (/admin/parametros, modo manual) fallaba en el servidor con un escueto "No se pudo actualizar el stock." tras unos 20 segundos de spinner. ESE MENSAJE NO ERA DEL SERVIDOR: es la cadena de respaldo de app.js, que se usa cuando la respuesta no trae la llave `mensaje`, o sea cuando la peticion muere antes de llegar al catch del controlador. Medido contra la API real, la corrida completa tardaba 47 s (token 3,4 s + ocho paginas de ~5 s), asi que IIS la cortaba a mitad: con QUEUE_CONNECTION=sync la sincronizacion ocurre DENTRO de la peticion y el set_time_limit(600) del controlador solo gobierna a PHP, no al FastCGI. La ejecucion del mismo comando por consola en produccion (sin --simular) termino bien y escribio 1.217 filas, lo que descarto codigo, red, credencial, criterios, escritura en SQL Server y permisos: fallaba unicamente el camino que pasa por IIS. LA CORRECCION ES UNA CONSTANTE, Y EL MOTIVO ES QUE EL COSTO DEL ERP ES POR LLAMADA Y NO POR FILA: probado contra el endpoint, una pagina de 1.000 filas tarda 5,28 s, una de 5.000 tarda 5,38 s y una de 10.000 tarda 4,62 s, asi que los ~7.100 items del inventario costaban ocho viajes de ida y vuelta (unos 40 s de pura latencia) y hoy caben en uno solo. InventarioApiSidocsa::TAMANO_PAGINA pasa de 1000 a 10000 y la corrida completa baja de 47 s a 9,6 s, verificado con repuestos:sincronizar-stock --simular contra la API real, con los mismos contadores que dio produccion (7.139 registros, 1.217 que cambian, 29 sin correspondencia): reducir los viajes no altera el resultado, solo el tiempo. Sigue siendo UNA sola constante para el `cant` que viaja y para el corte de la ultima pagina en LectorInventarioErp, por el motivo de siempre; MAX_PAGINAS = 50 pasa a ser un techo de 500.000 items y el pico de memoria sube de 28 a 38 MB (memory_limit 512M). SEGUNDO PROBLEMA, DERIVADO DEL PRIMERO: cuando el proceso muere de golpe, el `finally` de SincronizadorStockRepuestos::sincronizar() no corre y el candado y la marca `en_curso` quedan puestos hasta vencer, 15 minutos en los que el panel dice "Sincronizacion en curso..." y CADA intento nuevo rebota con 409 sin llamar al ERP -- el sistema se ve averiado mucho despues de que el fallo real ocurrio, y salir de ahi requeria borrar filas de cache_locks a mano, porque ni php artisan cache:clear sirve (el store de base de datos solo vacia la tabla `cache`, los candados viven en otra). Se agrega liberarBloqueo() en el servicio (que es el dueno del candado, para no decidir esto en dos sitios), la ruta POST admin/parametros/liberar-sincronizacion con permiso:parametros,editar, y el boton "Liberar bloqueo" en la tarjeta, contorneado y con data-confirmar porque es una palanca peligrosa: si de verdad hay una corrida trabajando, liberar el candado deja arrancar una segunda en paralelo. Se ofrece en los dos modos, porque una corrida del programador tambien puede morir a medias. La tarjeta pasa a decir DESDE CUANDO lleva la corrida y avisa a partir de los tres minutos: "en curso" a secas no distinguia una corrida recien arrancada de un candado colgado, que son dos situaciones opuestas (una hay que esperarla, la otra hay que liberarla). Y el aviso de error de app.js cae ahora a `message` y, si tampoco esta, al codigo HTTP: que el unico sintoma visible fuera una frase generica sin codigo ni pista es lo que mando la busqueda del fallo por el camino equivocado. Tres pruebas nuevas en ParametroAdminTest (liberar el bloqueo deja volver a sincronizar, el almacenista recibe 403, y la tarjeta dice desde cuando); las tres que fijan el tamano de pagina referencian la constante por nombre, asi que el cambio no las rompio. 160 pruebas en total, todas en verde, y pint limpio. | developer | V 1.4.3 | 2026-09-21 11:38

2026-09-21 07:45 | CAT-13: quinta pastilla "Sobre stock" en el listado /admin/repuestos (Catalogo e inventario), junto a Todos, Existencias bajas, Agotados e Inactivos. Cuenta y filtra los repuestos cuya existencia supera stock_maximo. EL SUPUESTO QUE DECIDE TODO, Y ESTA VERIFICADO CONTRA LA BASE REAL: stock_maximo en 0 (o nulo) es un maximo SIN DEFINIR y no un maximo de cero, asi que esas filas quedan fuera del conteo y del filtro. Sin ese recorte el indicador mostraria 4.592 repuestos "sobre stock" sobre 28.490; con el recorte muestra 703. La diferencia son las 25.086 filas que traen stock_maximo = 0 (hoy no hay ninguna con nulo): cualquiera de ellas con existencia > 0 habria entrado, y el indicador habria dicho que una sexta parte del catalogo esta excedida, que es justo el tipo de numero que nadie vuelve a mirar. La condicion se escribe como `stock_maximo > 0`, que en SQL Server tambien descarta el nulo sin un IS NOT NULL aparte (null > 0 no es cierto), y queda comentada en las dos copias. EL CONTEO VA EN LA MISMA CONSULTA QUE YA EXISTIA: se agrego un selectRaw mas a RepuestoAdminController::totales() -- `sum(case when stock_maximo > 0 and existencia > stock_maximo then 1 else 0 end) as sobre_stock` -- y no una consulta nueva, que era el motivo de que ese metodo exista: con 28.490 filas, cada COUNT suelto es un recorrido mas de la tabla en cada carga del listado (el docblock paso de "los cuatro conteos" a "los cinco"). El filtro se sumo al mismo encadenado de when() del index con la clave 'sobre_stock', con where('stock_maximo','>',0)->whereColumn('existencia','>','stock_maximo'); solo un filtro esta activo a la vez, asi que no hace falta agrupar las condiciones. LA VISTA NO GANO MARCADO NUEVO: admin/repuestos/index.blade.php pinta las pastillas recorriendo el arreglo $filtros, asi que el cambio es una entrada mas en ese arreglo y hereda enlace, badge, estado "seleccionado" y la conservacion del termino de busqueda. Queda al final, despues de Inactivos, que es donde la marco la imagen de referencia. COLOR DEL BADGE: 'success' (verde, --ca-exito via --bs-success), y la eleccion no es de gusto sino de descarte. 'danger' y 'primary' son los dos rojos y ya estan tomados por Agotados y por la marca, y CLAUDE.md pide no ponerlos a competir; 'warning' es Existencias bajas; 'dark' y 'secondary' son Todos e Inactivos; e 'info' quedaba descartado por contraste, porque la paleta lo reasigno a --ca-neutro-700 (#4a4642) mientras Bootstrap le deja el texto en negro, que sobre ese fondo no se lee. No se invento ningun hex ni se agrego CSS: el verde ya vive en los tokens. Ademas es lo que el color dice: un exceso no es una averia de despacho como el agotado. NO HAY OTRO SITIO QUE MANTENER SINCRONIZADO: se reviso el menu lateral (admin/partials/menu.blade.php) y no muestra conteos, y totales() solo lo consume esta pantalla. Nueva prueba tests/Feature/RepuestoSobreStockTest con 5 casos: el filtro trae el repuesto excedido, deja fuera el que esta dentro del maximo y el que no tiene maximo definido, el conteo sube en uno con un excedido y no se mueve con uno sin maximo. Los conteos se miden POR DIFERENCIA (antes y despues) y no contra un numero fijo, porque phpunit.xml apunta a la base real y el total absoluto cambia con los datos. Las tres pruebas del filtro miran viewData('repuestos') y no el HTML: el primer intento con assertDontSee sobre el codigo fallo porque el buscador reimprime el termino en el value del input, que es un falso positivo listo para confundir a quien toque esto despues. 157 pruebas en total, todas en verde, y pint limpio. | developer | V 1.4.2 | 2026-09-21 08:05

2026-09-20 15:12 | UI-03: quitar la fila de tarjetas de conteo por estado (Todas, Pendientes, En proceso, Listos, Entregadas) de la pantalla "Listos para reclamar" del panel, sin tocar la del listado general de Solicitudes. EL PROBLEMA ERA QUE LAS DOS ERAN LA MISMA PANTALLA: la entrada del menu apuntaba a admin.solicitudes.index con ?estado=listo, o sea la misma ruta, el mismo controlador y la misma vista, asi que no habia forma de distinguirlas por URL (y esconderlas con un if sobre request('estado') tambien habria borrado las tarjetas cuando el usuario llega ahi haciendo clic en la tarjeta "Listos" del listado general, que es filtrar y no cambiar de pantalla). SOLUCION: ruta propia GET admin/solicitudes/listos -> SolicitudAdminController::listos, nombre admin.solicitudes.listos, con el mismo middleware permiso:solicitudes,ver que el resto del modulo; va declarada ANTES de /{solicitud} para que el binding del modelo no la capture. El estado lo fija la RUTA y no la URL, y la vista recibe del controlador tres variables en vez de consultar el request: mostrarMetricas (false aqui, true en el listado general), rutaListado y parametrosBase, que son los que usan el formulario de busqueda y el boton de limpiar para volver a su propia pantalla. NO SE DUPLICO LA CONSULTA: index y listos llaman al metodo privado bandeja($estado, $termino), que conserva el mismo with, withCount, scope estado, scope buscar, el mismo orden por estado y fecha, paginate(15) y withQueryString; asi el filtrado, la busqueda, la paginacion y las acciones de cada fila quedan identicos. La consulta de conteos por estado (el groupBy) ya NO se ejecuta en la bandeja de listos, que era el unico motivo para hacerla. MENU: la entrada "Listos para reclamar" apunta a la ruta nueva y su marca de "estas aqui" pasa de comparar request('estado') a routeIs('admin.solicitudes.listos'); la entrada "Solicitudes" dejo de usar routeIs('admin.solicitudes.*') (que ahora tambien habria casado con listos) y se acota a index y show. La URL vieja /admin/solicitudes?estado=listo sigue funcionando y sigue mostrando las tarjetas: es el listado general filtrado, que es lo que corresponde. No quedaron partials ni CSS huerfanos: .tarjeta-metrica y las clases .text-estado-* las sigue usando el listado general. Se agrego tests/Feature/SolicitudesListosTest con 5 casos (la bandeja de listos no pinta las tarjetas, el listado general si las conserva, la bandeja solo muestra solicitudes en estado listo, la busqueda dentro de la bandeja no se lleva las de otros estados y el filtro por estado del listado general sigue funcionando). 152 pruebas en total, todas en verde, y pint limpio. | developer | V 1.4.1 | 2026-09-20 15:38

2026-09-14 11:38 | ADM-04: modulo Funciones por perfil (/admin/permisos), matriz de permisos ver/editar/eliminar por rol y por funcionalidad del panel, aplicada en el servidor a todas las rutas del panel. (1) BASE: tablas tbl_funcionalidad (col_clave unica, col_nombre, col_seccion, col_icono, col_orden, col_activo) y tbl_rol_funcionalidad (col_rol_id y col_funcionalidad_id con FK sin cascada, col_ver, col_editar, col_eliminar, indice unico rol + funcionalidad y CHECK ck_tbl_rol_funcionalidad_ver que exige ver cuando hay editar o eliminar). Las FK llevan prefijo col_ porque la tabla es nueva; el sin prefijo de CLAUDE.md aplica solo a las FK agregadas a tablas viejas. (2) DECISION: User::puede(funcionalidad, accion) es el UNICO punto de decision y lo usan el middleware nuevo permiso:clave,accion (EnsurePermiso, que registra cada acceso denegado), los authorize() de RepuestoRequest, CategoriaRequest, RolRequest y ParametroRequest (antes puedeGestionarCatalogo), la directiva Blade @puede y el Gate permiso. Orden: el rol del sistema admin puede todo siempre; un rol con matriz guardada obedece la matriz y una funcionalidad sin fila no concede nada; un rol SIN ninguna fila cae a Funcionalidad::permisoHeredado(), que reproduce exactamente lo que hacia es.admin; sin rol vigente se cae a users.rol. Es el mismo patron de cambio compatible de users.rol / rol_id: correr solo migrate no deja a nadie por fuera. Una consulta por instancia de usuario (cache en el modelo). (3) CONVIVENCIA CON es.admin: todas las rutas del panel pasaron de es.admin a permiso: con ver para listar y abrir, editar para crear, modificar, ajustar existencia, tomar, elaborar, entregar, reenviar aviso y sincronizar, y eliminar para anular, desactivar y rechazar. Se reemplazo y no se sumo porque con los dos la matriz solo podria quitar y nunca conceder, y la pantalla mentiria. El alias es.admin y puedeGestionarCatalogo() siguen registrados sin uso en rutas; col_gestiona_catalogo queda como respaldo de un rol sin matriz, y el formulario de rol lo dice. (4) PROTECCION DEL ADMIN EN EL SERVIDOR: puede() le devuelve true pase lo que pase en la base, y PermisoService::guardar() le escribe todo en true aunque llegue un formulario vacio; la vista ademas bloquea sus casillas. Guardar exige permiso:permisos,editar en la ruta y en PermisoRolRequest: ver la matriz no alcanza. (5) SERVICIO: PermisoService relee el rol con lockForUpdate (rechaza uno anulado), normaliza editar/eliminar implican ver, escribe una fila por CADA funcionalidad activa (marcada o no, que es lo que apaga la herencia) con MERGE nativo with (holdlock) en lotes de 300 filas por el limite de 2100 parametros, y registra el cambio en Log::info. PermisoRolRequest valida rol activo existente, que las llaves sean ids de funcionalidades activas y que solo lleguen ver, editar y eliminar. (6) SEEDER FuncionalidadSeeder idempotente: MERGE por col_clave de las seis funcionalidades reales (solicitudes, repuestos, categorias, roles, permisos, parametros; no existe modulo de usuarios) refrescando nombre, seccion, icono y orden pero nunca col_activo, y matriz inicial SOLO para los roles sin ninguna fila, con el permiso heredado: admin todo, almacenista solo solicitudes. Agregado a DatabaseSeeder despues de UsuarioSeeder. (7) INTERFAZ: vista admin/permisos/index con select de perfil (data-autoenviar), tarjetas por seccion en grilla 1/2/3 columnas, fondo tenue de la marca en las que tienen algun permiso, Seleccionar todo, Limpiar y avisos para el admin, el rol sin matriz guardada y el usuario que solo puede ver. app.js: form[data-matriz-permisos] aplica editar/eliminar implican ver y pinta la tarjeta. app.css: .tarjeta-permiso, .seccion-permisos, .selector-perfil y .accion-sin-permiso con tokens --ca-*. Parcial admin.partials.accion-sin-permiso: boton gris deshabilitado sin formulario ni enlace detras, aplicado en los listados de repuestos, categorias, roles y parametros y en el detalle de la solicitud; el menu lateral solo muestra lo que el perfil puede ver y tiene la entrada Funciones por perfil. (8) PRUEBAS: tests/Feature/PermisoAdminTest con 21 casos (guardar matriz sin duplicar, editar implica ver, CHECK en la base, admin no pierde permisos ni borrados a mano, casillas del admin bloqueadas, almacenista 403 al ver y al guardar, ver no alcanza para guardar, middleware bloquea lo no marcado, la matriz concede lo que es.admin no daba, boton sin permiso deshabilitado, detalle de solicitud, todos los listados con y sin permisos, funcionalidad nueva desmarcada, rol sin matriz conserva la herencia, rol anulado, llaves y acciones desconocidas, invitado). 147 pruebas en total, todas en verde (las 126 anteriores intactas + 21 nuevas), y pint limpio. Migraciones y seeder corridos sobre la base real. PENDIENTES: revision visual en el navegador no hecha; un perfil sin permiso de ver solicitudes cae en 403 despues del login porque el login redirige a solicitudes; paso 2 (retirar es.admin y col_gestiona_catalogo) a decidir. | developer | V 1.4.0 | 2026-09-14 14:35

2026-09-14 14:50 | CAT-12: en el catalogo publico los filtros "Categoria" (categoria_id -> id_categoria) y "Tipo de repuesto" (categoria -> desc_cat_1) pasan a ser excluyentes. Un filtro esta activo cuando tiene un valor distinto de la opcion vacia; mientras uno lo tiene, el otro se pinta deshabilitado, y con los dos vacios ambos quedan habilitados. NO CAMBIO COMO FILTRA NINGUNO: mismas columnas, mismas opciones, mismos nombres de parametro y el mismo autoenvio. (1) VISTA catalogo/index: @disabled en el render inicial segun el request y un texto de ayuda por select (enlazado con aria-describedby) que dice por que esta bloqueado y como liberarlo; va oculto con hidden mientras no hay bloqueo. La fila de filtros paso de align-items-center a align-items-start (y el interruptor de pt-lg-2 a pt-sm-2) para que la altura del texto de ayuda no desplace el buscador y el interruptor. (2) app.js: nuevo atributo generico data-excluye="id-del-otro-campo" por delegacion, igual que el resto de atributos data-. Al cambiar, deshabilita o habilita el otro campo y muestra u oculta su ayuda; tambien se reaplica al cargar porque el navegador puede restaurar el valor de un select al volver con "atras" sin restaurar el disabled. ORDEN DE EVENTOS: el bloque se registra ANTES que data-autoenviar, porque los listeners de un mismo elemento corren en orden de registro y form.submit() arma los datos al llamarse; al reves viajarian los dos valores. Un select deshabilitado no viaja en el formulario, que es lo que garantiza que llegue uno solo. (3) app.css: .form-select[data-excluye]:disabled con tokens --ca-* (fondo neutro-200, texto terciario, cursor not-allowed); sin esto el :disabled de Bootstrap cae en #e9ecef, casi igual al fondo hundido de los inputs, y el bloqueo no se veia. Acotado a [data-excluye] para no alterar los campos deshabilitados del panel. (4) DEFENSA EN SERVIDOR, CatalogoController::index: si llegan los dos por URL manipulada MANDA LA CATEGORIA y el tipo se ignora, incluido en los enlaces de la paginacion (withQueryString se reemplazo por appends sobre la consulta depurada, que es exactamente lo que hacia withQueryString). Con un solo parametro el comportamiento es identico al anterior, y un categoria_id vacio o no numerico sigue sin ser filtro, asi que no anula el tipo. Se agrego tests/Feature/CatalogoFiltrosExcluyentesTest con 8 casos: sin filtros ambos habilitados, con categoria el tipo deshabilitado, con tipo la categoria deshabilitada, el tipo solo sigue filtrando, un categoria_id no numerico no anula el tipo, con los dos manda la categoria y el tipo no queda seleccionado, la paginacion no arrastra el tipo ignorado y con un solo parametro conserva la consulta. 126 pruebas en total, todas en verde, y pint limpio. PENDIENTE CONOCIDO: un categoria_id que no esta en la lista (inactivo o inexistente) sigue filtrando como antes y bloquea el tipo, aunque el select muestre "Todas las categorias"; se sale con "Limpiar filtros". | developer | V 1.3.11 | 2026-09-14 15:20

2026-09-13 09:10 | CORRECCION: cambiar la foto de un repuesto desde el panel no se veia en el servidor de produccion (IIS). El formulario decia "Repuesto actualizado", el archivo quedaba en public/img y repuestos.foto guardaba su nombre, pero el catalogo mostraba la imagen rota. CAUSA CONFIRMADA: GET /img/103-20260913135436.jpeg respondia 401 mientras las fotos copiadas a mano respondian 200. RepuestoAdminController::guardarImagen() usaba $imagen->move(), que en Windows es un rename desde el directorio temporal de PHP, y en NTFS un rename dentro del mismo volumen CONSERVA las ACL del origen: el archivo llegaba a public/img sin permiso de lectura para IUSR (por eso su propietario salia como SYSTEM) e IIS lo negaba. La correccion copia el contenido a un archivo NUEVO en public/img (fopen 'xb' + stream_copy_to_stream), que hereda los permisos de la carpeta destino; se verifico en Windows que el archivo movido no trae IUSR y el copiado si (IUSR:(I)(R)). Si la copia falla se borra el archivo parcial y se lanza la excepcion en vez de guardar un nombre que apunte a nada. Se agrego tests/Feature/RepuestoFotoAdminTest (2 casos: la foto nueva llega completa y queda asociada; guardar sin imagen conserva la anterior). LAS FOTOS YA SUBIDAS SIGUEN CON LA ACL VIEJA: hay que resetearlas una vez en el servidor con icacls "C:\inetpub\wwwroot\cat_alm\public\img" /reset /T. | developer | V 1.3.10 | 2026-09-13 09:25

2026-08-26 08:40 | CAT-11: carga inicial de repuestos.existencia copiando repuestos.stock, con el comando repuestos:inicializar-existencia-desde-stock. La tabla repuestos se recreo con el esquema del ERP y existencia (el saldo operativo del almacen) quedo en 0 en las 28.490 filas, asi que el catalogo no dejaba armar un solo pedido: el Carrito topa contra existencia y SolicitudService la relee dentro de la transaccion. Ya existia una via para desbloquearlo, InicializadorExistenciaRepuestos, que lee la API del ERP; esta es una segunda via que no depende de que la API responda. EL RIESGO SE ASUMIO A SABIENDAS Y HAY QUE DEJARLO ESCRITO: stock no es solo lo que confirma la API, arrastra tambien los valores de la carga masiva del ERP del 2026-08-21, y en unas 2.669 filas ese valor es un punto de reposicion (stock == stock_minimo con stock_maximo al doble) y no una existencia fisica, asi que copiarlo le da saldo en el catalogo a repuestos que en la estanteria pueden no tenerlo. Se pregunto explicitamente si excluir esas filas y LA DECISION FUE COPIAR stock TAL CUAL SIN EXCLUIRLAS, porque la alternativa era dejar el almacen sin despachar esperando a la API; el ajuste fino se hace despues repuesto por repuesto desde el panel. Queda anotado en el docblock del servicio, en el aviso del comando y en CLAUDE.md para que quien venga despues no tenga que volver a discutirlo pero si sepa que un saldo inicial puede no corresponder a una existencia real. POR QUE ES UNA CLASE HERMANA Y NO UN MODO DE LA QUE YA EXISTIA: por el mismo motivo por el que aquella se separo del sincronizador, dos escrituras irreversibles del saldo operativo detras del mismo if quedan a una edicion desafortunada de distancia entre "copiar el stock" y "escribir lo que dice la API"; separadas, cada comando hace una sola cosa y se teclea con su propio nombre. Ademas no comparten casi nada: aquella lee la API, cruza en PHP y escribe agrupando por cantidad, y esta resuelve el caso entero en UNA sentencia SQL. Lo unico verdaderamente comun es la bandera, y por eso la bandera se subio al modelo: Parametro::existenciaYaInicializada() es ahora el UNICO lector de inv.existencia_inicializada y Parametro::marcarExistenciaInicializada() el unico escritor, y las dos vias los usan. EL HECHO "existencia YA RECIBIO SU CARGA INICIAL" ES UNO SOLO Y NO PUEDE TENER DOS BANDERAS: si una via ya inicializo, la otra no corre. El respaldo se mantiene invertido respecto al de inv.actualizar y es deliberado: si el parametro falta o esta inactivo se da por inicializada y NO se escribe, porque aqui lo seguro es no tocar el saldo. TRES BARRERAS, porque el error no tiene vuelta atras: (1) la bandera durable, que vive en la base y sobrevive a un cache:clear; (2) el UPDATE toca unicamente las filas con existencia = 0, asi que una fila con saldo vivo no se pisa jamas ni aunque alguien repita el comando, y salta las filas con stock <= 0 porque escribir un 0 sobre un 0 solo mueve la fecha; (3) la confirmacion interactiva, que ademas imprime el aviso de los puntos de reposicion. Todo el trabajo va en UNA sentencia (update ... set existencia = stock where existencia = 0 and stock > 0) dentro de una transaccion junto a la marca de la bandera: son 28.490 filas, no hay lista de codigos que partir, asi que el limite de 2100 parametros de SQL Server no aplica y traerlas a memoria seria gastar minutos en lo que la base hace de una. La columna de origen se cita con la gramatica del conector en vez de interpolar texto. --simular reporta sin escribir nada, ni el saldo ni la bandera. Los cuatro contadores son excluyentes y exhaustivos (total = yaConSaldo + candidatas + sinStock) para que un descuadre se vea a simple vista. SIMULACION SOBRE LA BASE REAL: 28.490 repuestos, 7.229 recibirian saldo, 0 con saldo previo, 21.261 siguen en 0 porque el ERP no reporta stock. Se agrego tests/Feature/InicializarExistenciaDesdeStockTest con 10 casos: copia en las filas en cero, no pisa un saldo vivo, copia el valor exacto con decimales, salta las filas sin stock, no toca stock ni ninguna otra columna, --simular no escribe ni el saldo ni la bandera, la bandera bloquea una segunda corrida, sin el parametro no corre, la bandera es la misma de la via por API, y los contadores cuadran con el total. 116 pruebas en total, todas en verde, y pint limpio. LA ESCRITURA REAL NO SE EJECUTO: queda a cargo del administrador. | developer | V 1.3.9 | 2026-08-26 09:32

2026-08-25 18:30 | CAT-10: los ultimos dos archivos que seguian llamando cantidad_disponible a la columna existencia. La tabla repuestos se recreo con el esquema del ERP y cantidad_disponible paso a llamarse existencia (decimal(12,3) not null default 0); el grueso del codigo ya estaba realineado, pero quedaban tres sitios escribiendo el nombre viejo. (1) App\Services\GeneradorDatosRepuesto devolvia la clave cantidad_disponible: se renombro a existencia y se ajusto la firma @return del array shape. NO se toco el algoritmo ni un solo valor: la cantidad sigue saliendo del mismo crc32 del codigo, porque cambiarla habria alterado los repuestos ya publicados al volver a sembrar. El phpdoc deja dicho que la columna es decimal(12,3) y que el entero que produce el generador es un decimal valido, para que nadie lo "corrija" despues. Se cambio ademas la palabra "stock" por "existencia" en dos comentarios de la clase: stock es hoy otra columna real y dejarla ahi era invitar a escribir en la equivocada. (2) App\Console\Commands\ImportarRepuestos usaba el nombre viejo en tres puntos (la descripcion del comando, la lista COLUMNAS y el foreach de conversion numerica). DECISION SOBRE LA COMPATIBILIDAD DEL CSV: los archivos que el almacen ya tiene armados traen la cabecera cantidad_disponible, asi que se ACEPTAN LAS DOS y el mapa ImportarRepuestos::ALIAS traduce la vieja al leer el encabezado, una sola vez y en un solo sitio; obligar a reeditar a mano cada CSV existente no compraba nada. El alias NO se aplica si el archivo ya trae la columna nueva: con las dos presentes manda existencia y la vieja se ignora como cualquier columna desconocida, porque dejar que dos columnas escriban el mismo campo hace que el resultado dependa del orden de las columnas. (3) LA CONVERSION NUMERICA ESTABA MAL Y ERA UN TRUNCAMIENTO SILENCIOSO: el importador hacia (int) sobre las dos cantidades, y existencia y stock_minimo son decimal(12,3) porque el almacen mide en KG y hay items con fraccion; 12,5 KG entraban como 12. Ahora se leen como decimales, admitiendo la coma como separador (el separador de columnas por defecto es ";", justo el del Excel en espanol, y ese Excel escribe "12,5" y no "12.5"), y UN VALOR NO NUMERICO YA NO ENTRA COMO 0: un (float) 'abc' da 0.0 y le habria borrado el saldo a un item real sin decir nada, asi que el campo se descarta con un aviso que dice linea y columna, y el resto de la fila si se importa. (4) App\Console\Commands\SincronizarImagenesSupabase escribia el manifiesto CSV con la cabecera cantidad_disponible; como ese archivo se importa con repuestos:importar, salia con una cabecera que el importador ya no reconocia. Quedo en existencia. NI EL IMPORTADOR NI EL GENERADOR TOCAN stock: stock lo escribe unicamente la sincronizacion con el ERP y existencia es el saldo operativo; stock ni siquiera esta entre las columnas admitidas del CSV y hay una prueba que lo sostiene. HALLAZGO QUE QUEDA PENDIENTE Y NO SE ARREGLO AQUI: database/seeders/RepuestoSeeder sigue roto por otra razon distinta del nombre de la columna. Hace upsert con descripcion, categoria, activo, created_at y updated_at, cinco columnas que el esquema del ERP ya no tiene, y el upsert va por el query builder, asi que $fillable no lo filtra y revienta con Invalid column name; el mismo GeneradorDatosRepuesto sigue devolviendo esas claves. No se toco porque arreglarlo exige antes una decision de negocio que no es tecnica: con el catalogo real del ERP ya cargado (28.490 filas), correr el seeder sobrescribiria los nombres reales del almacen con los genericos que inventa el crc32, asi que primero hay que decidir si ese seeder todavia tiene sentido. Queda anotado en CLAUDE.md. Se agrego tests/Feature/ImportarRepuestosTest, que hasta ahora no existia: 6 casos que cubren el encabezado viejo, el nuevo, los dos a la vez, el decimal con coma y con punto, el valor no numerico que no borra el saldo y la invariante de que la importacion no toca stock. 106 pruebas en total, todas en verde, y pint limpio. CLAUDE.md se actualizo en cuatro puntos donde todavia decia cantidad_disponible o llamaba "stock" a la existencia generada. | developer | V 1.3.8 | 2026-08-25 18:55

2026-08-24 20:35 | CAT-09: el boton "Actualizar" de /admin/parametros decia la verdad a medias y la hora estaba corrida cinco horas; ademas cada corrida deja ahora una bitacora en texto. TRES ARREGLOS Y UNA PIEZA NUEVA. (1) EL CONTADOR "ACTUALIZADOS" NO CONTABA LO QUE DECIA: SincronizadorStockRepuestos::escribirStock devuelve las filas que el UPDATE afecta, y en SQL Server un UPDATE afecta TODAS las filas que empareja, cambien o no de valor, asi que el numero era "repuestos escritos" y no "repuestos actualizados"; salia identico (7.092 contra el ERP real) tanto en una corrida con novedades como en una que no movio un solo item, y encima reescribia miles de filas identicas y les tocaba updated_at en cada corrida. Ahora cruzarContraElCatalogo trae tambien el stock que ya hay —misma consulta, mismo indice, una columna mas— y separa lo que de verdad cambia (`actualizados`) de lo que ya estaba al dia (`sinCambio`, contador nuevo); solo lo primero entra en el UPDATE. La comparacion lleva una TOLERANCIA de 0.0005 porque stock es decimal(12,3) y son floats: sin margen, un item que el ERP reporta como 12.5001 se reescribiria para siempre contra el 12.500 que la columna puede guardar. Un stock NULO no se convierte a 0: cuenta como distinto de cualquier cantidad para que la primera corrida se lo escriba. Medido contra el ERP real: primera corrida 37 cambiados y 7.044 ya al dia; segunda corrida seguida, 0 cambiados y 7.081 ya al dia. (2) EL MENSAJE DEL TOAST DEJO DE SER UNA SOLA FRASE CON NUMEROS DENTRO: un "0 repuesto(s) actualizado(s)" no distinguia tres situaciones que son dos averias y una normalidad, y quien aprieta el boton no tiene el log a mano; ResultadoSincronizacionStock::resumen() tiene ahora una frase por caso — el ERP no devolvio nada (y apunta a api.id_bod / api.criterio / api.criterio_2), ninguno de los N registros cruzo con el catalogo, el stock ya estaba al dia, o cuantos cambiaron. (3) LA HORA DE "ULTIMA CORRIDA" ERA UTC: la marca del cache es un timestamp unix (bien) pero se leia con config('app.timezone'), que en este proyecto es UTC, asi que el panel decia "11:38 pm" para una corrida que en Colombia ocurrio a las 6:38 pm; el administrador leia esa hora imposible como que la marca no se habia movido. ultimaCorrida() y proximaCorrida() se leen ahora en America/Bogota (constante ZONA_VISIBLE); NO se toco config/app.php ni la zona de la aplicacion, que sigue siendo UTC, porque cambiarla mezclaria los created_at ya guardados con los nuevos. Ademas la fecha vuelve en el JSON del boton (`ultima`, ya formateada por ultimaCorridaFormateada(), un solo formato para la vista y para el JSON) y app.js la pinta en [data-sincronizar-ultima] al instante, con el mismo patron de delegacion por data- que ya usaba; se elimino el window.location.reload() a los 4,2 segundos, que borraba el aviso y dejaba al usuario cuatro segundos mirando la fecha vieja. (4) BITACORA NUEVA storage/logs/sincronizacion.txt (sin tilde, por la convencion de espanol sin tildes del proyecto): una linea por corrida con fecha y hora de Colombia, items actualizados e items NO actualizados con su desglose (ya al dia, sin correspondencia, sin item, item no numerico, sin cantidad). VA EN storage/logs Y NO EN LA RAIZ junto a changelog.txt porque es salida de ejecucion y no documentacion versionada; storage/logs/.gitignore ya la deja fuera de git. La escribe App\Services\BitacoraSincronizacionStock, a la que llama UNICAMENTE el servicio orquestador, de modo que la consola y el boton del panel escriben lo mismo sin duplicar nada. Se ANEXA con FILE_APPEND|LOCK_EX y crea el archivo la primera vez con un encabezado que explica las columnas; en --simular NO escribe (una simulacion no deja rastro en ninguna parte: ni stock, ni marca, ni bitacora); y un fallo al escribirla NUNCA tumba la sincronizacion, se captura y se registra en Log, mismo criterio que el correo de "pedido listo". Ni codigo_api ni el token entran en el archivo: solo contadores, ni siquiera los codigos de los repuestos. Bajo PHPUnit la bitacora se escribe en sincronizacion-pruebas.txt (ARCHIVO_PRUEBAS): las pruebas corren la sincronizacion de verdad contra un Http::fake() y sin esa separacion cada `php artisan test` metia decenas de renglones de juguete en el archivo que lee el almacen. 6 pruebas nuevas (5 en SincronizarStockTest y 1 en ParametroAdminTest), incluida la del fallo de escritura provocado de verdad poniendo una carpeta en el lugar del archivo; 100 pruebas en total, todas en verde, y pint limpio. Las dos pruebas que sostienen la invariante (test_la_sincronizacion_no_toca_existencia y test_la_sincronizacion_manual_escribe_stock_y_no_existencia) siguen pasando: la sincronizacion escribe unicamente repuestos.stock. | developer | V 1.3.7 | 2026-08-24 21:10

2026-08-23 09:10 | CAT-08: nueva estructura del cuerpo de POST /api/v1/inventario/consultar. TRES CAMBIOS DE CONTRATO Y NADA MAS: (1) cant_page DESAPARECE del cuerpo; (2) criterio (SUBGRUPO) pasa de nombres a IDs NUMERICOS; (3) criterio_2 (GRUPO) lo mismo. Ademas cant sube de 500 a 1000 y cant/page dejan de ir entrecomillados: viajan como numeros JSON, mientras id_cia, id_bod y tipo_inv siguen siendo cadenas. El cuerpo queda literalmente {"cant":1000,"page":1,"id_cia":"1","id_bod":"P2ALM","tipo_inv":"INV1455","criterio":[3038,1230],"criterio_2":[2002,2010,2013,2014],"existencias":1} y se sigue armando en UN SOLO SITIO, InventarioApiSidocsa::pedirInventario(), asi que la consola y el boton "Actualizar" del panel mandan exactamente la misma peticion. LA PAGINACION ERA LA CONSECUENCIA PELIGROSA DEL CAMBIO: CANT_PAGE=100 cumplia dos papeles a la vez, iba en el cuerpo como cant_page Y era el corte de la ultima pagina en LectorInventarioErp; al desaparecer cant_page el tamano de pagina pasa a ser cant, y si el corte se hubiera quedado en 100 el recorrido habria terminado en la primera pagina —que ahora trae 1000— dejando el inventario a medias SIN UN SOLO ERROR VISIBLE. Por eso las dos constantes se fusionaron en UNA, InventarioApiSidocsa::TAMANO_PAGINA = 1000, que es el valor que viaja en cant y el corte de la ultima pagina; queda un solo numero que no puede divergir consigo mismo, y esta explicado en el comentario de la constante y en el punto donde se usa el corte. El comentario viejo de CANT (que explicaba que cant NO era el paginador y que se habia probado con 2000) se elimino: dejo de ser cierto. MAX_PAGINAS baja de 500 a 50 conservando el mismo techo real: 50 paginas de 1.000 son los mismos 50.000 items que antes daban 500 de 100, holgado contra los ~29.000 del ERP; el tope existe solo para que un endpoint que responda lleno siempre no deje el comando girando, y con paginas 10 veces mas grandes mantener 500 lo habria multiplicado por diez sin razon. LOS CRITERIOS AHORA SON NUMEROS, y eso obligo a un lector nuevo: Parametro::listaEnteros(), que reusa lista() (o sea que sigue leyendo por Parametro::valor() y un criterio inactivo devuelve []), le hace trim a cada elemento y castea a int. EL trim ES UN CAMBIO DELIBERADO Y ACOTADO: lista() no lo hacia porque el nombre de un grupo podia llevar un espacio significativo, razon que desaparece con un ID; la regla general sigue intacta y col_valor SIGUE excluido del middleware TrimStrings en bootstrap/app.php, porque otros parametros si dependen de ella. Se exige ctype_digit y no is_numeric: (int) "ELECTRICO" daria 0, que para la API es un filtro valido, asi que un elemento no numerico se DESCARTA en vez de colarse como cero. El mecanismo quedo GENERICO, no escondido en el servicio ni en la vista: Parametro::LISTAS_ENTERAS es el subconjunto de LISTAS cuyos elementos son IDs, Parametro::esListaEntera() su unico lector y el accesor es_lista_entera lo que consulta el formulario. VALIDACION EN EL SERVIDOR, mismo espiritu que Rule::in para OPCIONES: ParametroRequest rechaza con regex un valor que no sea una lista de digitos separados por coma para esos dos parametros (probado con "ELECTRICO,MATERIAS PRIMAS", que vuelve con error y deja el valor anterior intacto); admite espacios alrededor de cada ID porque listaEnteros() los recorta, y admite el vacio porque vacio significa "sin filtro". El formulario de /admin/parametros cambio la ayuda del campo: ya no habla de nombres de grupo ni de espacios significativos, sino de IDs numericos con el ejemplo 3038,1230. EL RIESGO DE FALLO SILENCIOSO SE ATACO POR DOS FRENTES, porque solo el log no alcanzaba: ParametroSeeder hace upsert por col_nombre y NO pisa el valor, asi que una instalacion que ya existe se habria quedado con los nombres de grupo, listaEnteros() los habria descartado todos y la consulta habria salido SIN FILTRO DE GRUPO, trayendo un universo de items que no es el acordado y sin un solo error en pantalla. (a) Cada descarte se registra con Log::warning nombrando el parametro, cuantos elementos se descartaron y hasta 5 ejemplos —nunca de un parametro SENSIBLES, para no escribir jamas una credencial en el log—. (b) La migracion 2026_08_23_100000_actualizar_criterios_api_a_ids reemplaza el valor de api.criterio y api.criterio_2 UNICAMENTE cuando no aporta ni un solo ID numerico, que cubre tanto los nombres viejos como el vacio con que se sembraba api.criterio bajo el contrato anterior; un valor que ya trae IDs no se toca, porque ese si es configuracion hecha bajo el contrato nuevo. La migracion es de datos, va por SQL nativo parametrizado y su down() queda vacio a proposito: los nombres no se reconstruyen desde los IDs y devolverlos dejaria la sincronizacion sin filtro otra vez. Corrida contra la base real: api.criterio_2 traia ocho nombres de grupo y quedo en 2002,2010,2013,2014; api.criterio estaba vacio y quedo en 3038,1230; ambos leidos de vuelta con listaEnteros() dan exactamente los arreglos del ejemplo. Los valores sembrados por ParametroSeeder y sus descripciones se actualizaron a esos mismos IDs para las instalaciones nuevas. NO SE TOCO: la logica del token ni el reintento por 401, extraerCodigo(), extraerCantidad() ni filas() (la RESPUESTA de la API no cambia), SincronizadorStockRepuestos, y sobre todo la regla inviolable de que la sincronizacion escribe UNICAMENTE repuestos.stock y jamas existencia; ni codigo_api ni el token pasan por el cuerpo, por el log ni por la salida. Pruebas: en SincronizarStockTest se actualizaron las cuatro que afirmaban el cuerpo exacto (criterios numericos, ausencia de cant_page, cant y page como numeros comprobados sobre el JSON crudo porque data() no distingue "1000" de 1000, y el uso de TAMANO_PAGINA como pagina llena) y se agregaron tres: que una lista con espacios y elementos vacios da [2002,2010], que un elemento no numerico se descarta sin colarse como 0 y que un criterio todo texto queda vacio. En ParametroAdminTest se agregaron dos: que el servidor rechaza un nombre de grupo y acepta "2002, 2010" y el vacio, y que el formulario dice que son IDs numericos. Se corrigieron ademas dos referencias obsoletas a api.criterio_2 como ejemplo del espacio significativo. Suite completa en verde: 92 pruebas, 280 aserciones, y Pint limpio. Nunca se llamo al endpoint real: todo con Http::fake(). | developer | V 1.3.6 | 2026-08-23 10:35

2026-08-20 20:50 | CAT-06 (ajuste del contrato): la peticion a POST /api/v1/inventario/consultar se pone al dia con lo que el ERP espera hoy. Cambian TRES cosas y ninguna mas: (1) criterio_2 (GRUPO) deja de viajar como cadena y viaja como ARREGLO JSON; (2) aparece criterio (SUBGRUPO), que antes no se enviaba y ahora viaja SIEMPRE, aunque sea vacio, porque el endpoint espera la llave; (3) el valor sembrado de api.id_bod pasa del marcador de posicion P0XXX a la bodega real P2ALM. cant, cant_page, page, existencias y tipo_inv ya coincidian con el payload acordado y no se tocaron; tipo_inv sigue siendo la constante INV1455 dentro de InventarioApiSidocsa. EL FORMATO DEL VALOR ES LA DECISION DE FONDO: en tbl_parametro un valor es una sola cadena y la API quiere varios grupos, asi que se acordo la COMA como separador ("ELECTRICO,MATERIAS PRIMAS") y se dejo escrito en tres sitios donde el proximo desarrollador si lo va a leer: la constante Parametro::LISTAS, la descripcion de los dos parametros en ParametroSeeder y la ayuda del campo en el formulario de /admin/parametros, que ahora dice como escribirlo cuando el parametro es de lista. El mecanismo quedo GENERICO y no como un caso especial escondido en el servicio, igual que se hizo con Parametro::OPCIONES: Parametro::LISTAS enumera que parametros son listas, Parametro::lista() es el unico que las parte y el accesor es_lista es lo que consulta la vista. Parametro::lista() lee por Parametro::valor(), asi que un criterio inactivo devuelve lista vacia y no se cuela como filtro. NO SE LE HACE trim A CADA ELEMENTO Y ES A PROPOSITO: col_valor esta excluido del middleware TrimStrings justamente porque un valor puede terminar en un espacio significativo para la API, y recortar los elementos al partir la lista habria reintroducido por la puerta de atras el mismo bug que esa excepcion arreglo; lo unico que se descarta son los elementos vacios, para que ni "" ni "A," manden cadenas vacias dentro del arreglo. api.criterio se siembra VACIO porque en la consulta de referencia el arreglo criterio va vacio, pero se siembra igual (con col_sistema = true, como sus hermanos) para que filtrar por subgrupo algun dia sea configurar el panel y no editar codigo. El valor sembrado de api.criterio_2 pasa a "ELECTRICO,MATERIAS PRIMAS" para que una instalacion nueva salga con la consulta de referencia; SE RESPETA LA REGLA DEL SEEDER: el upsert por col_nombre sigue refrescando SOLO la descripcion, de modo que el cambio de P0XXX a P2ALM y el de criterio_2 aplican a instalaciones nuevas y quien ya los tenga configurados los cambia desde /admin/parametros. Se comprobo al correr el seeder contra la base real: api.criterio entro nuevo y vacio, y los valores ya configurados no se movieron. El reparto en tres piezas no se toco: el cuerpo se sigue armando en un solo sitio (InventarioApiSidocsa::pedirInventario), asi que el comando de consola y el boton "Actualizar" del panel mandan exactamente la misma peticion. Tampoco se agrego ningun log del cuerpo: ni codigo_api ni el token pasan por ahi. Cuatro pruebas nuevas en SincronizarStockTest con Http::fake() + Http::assertSent(): que criterio y criterio_2 salen como arreglos con varios grupos, que vacios salen como [] (comprobado sobre el JSON crudo, porque un arreglo vacio de PHP tambien podria serializarse como {}), que los espacios de cada elemento sobreviven y los elementos vacios se descartan, y que un criterio inactivo no viaja. Nunca se llamo al endpoint real. Suite completa en verde (82 pruebas) y Pint limpio. | developer | V 1.3.5 | 2026-08-20 21:15

2026-08-20 15:10 | UI-02 (correccion): la marca de agua no se veia. La causa eran dos decisiones que juntas la volvian invisible en la practica: opacidad .06 y z-index -1. Con z-index negativo la marca queda por debajo de todo lo que trae fondo propio, y las superficies del proyecto (--ca-superficie) son blancas y OPACAS, asi que en el catalogo las tarjetas cubren casi toda la ventana y del logo solo asomaba un manchon en los huecos entre tarjetas; encima, .06 de opacidad sobre el lienzo #fafafa es practicamente indistinguible. Se comprobo en el navegador antes de tocar nada: el <img> estaba en el DOM, la imagen cargaba (238x105, servida desde /logo.png) y el elemento estaba bien posicionado, o sea que no era un fallo de ruta ni del include sino de visibilidad. SE INVIERTE LA CAPA: .marca-agua pasa de z-index -1 a 990, es decir ENCIMA del contenido, que es lo unico que la hace visible en paginas llenas de tarjetas opacas. 990 se eligio para quedar sobre todo el contenido normal pero POR DEBAJO de las capas de Bootstrap que si deben taparla: dropdown 1000, navbar sticky 1020, offcanvas 1045, modal 1055 y la zona de toasts 1090; asi la marca nunca se pinta sobre un menu abierto, un modal ni un aviso. El <img> sigue siendo hijo directo de <body> por la misma razon de antes: dentro de un contenedor con transform, opacidad o filtro ese contenedor le crearia un contexto de apilamiento y la marca quedaria atrapada en el. Al pasar la marca al frente la opacidad es el UNICO freno que queda, asi que sube de .06 a .10 y el criterio se topa en .14: por encima de ahi el rojo del logo compite con el texto que queda debajo. El ancho sube de min(58vw,360px) a min(70vw,520px) para que se lea como sello y no como un logo pequeno perdido al centro; sigue topado con max-width calc(100vw - 2rem), asi que no saca barra horizontal en movil. Solo cambian los dos tokens --ca-marca-agua-opacidad y --ca-marca-agua-ancho y la regla .marca-agua en public/assets/css/app.css, mas los comentarios, que decian lo contrario de lo que ahora hace el codigo. NO se toco el partial, ni los tres layouts, ni el @media print (la marca se sigue sin imprimir), ni pointer-events:none, asi que no roba un solo clic. Verificado en el navegador con el CSS real: el logo se lee centrado en / y en /admin/login. | claude-opus-5 | V 1.3.4 |

2026-08-20 14:25 | UI-02: marca de agua con el logo de la empresa (public/logo.png) en todas las paginas de la aplicacion. UNA SOLA PIEZA REUTILIZADA, no tres copias: la vista resources/views/partials/marca-agua.blade.php trae el <img> y se incluye en los TRES documentos HTML que existen en el proyecto, que son layouts/app.blade.php (publico), layouts/admin.blade.php (panel) y admin/login.blade.php (el ingreso no extiende ningun layout, es un documento suelto). Las demas vistas ya extienden uno de los dos layouts y quedan cubiertas sin tocarlas, incluida solicitudes/confirmacion. welcome.blade.php se dejo intacta a proposito: ninguna ruta la usa y esta fuera de la paleta. LA URL SALE DE asset('logo.png') EN EL BLADE y no de una ruta relativa escrita en el CSS, para que resuelva igual con php artisan serve y con Apache/XAMPP colgando de una subcarpeta, que es donde una ruta relativa desde public/assets/css/ se rompe en cuanto alguien mueve la hoja de sitio. EL <img> VA COMO HIJO DIRECTO DE <body>, Y ESO NO ES CASUAL: el estilo lo manda al fondo con z-index -1 y si estuviera dentro de un contenedor con transform, opacidad o filtro ese contenedor le crearia un contexto de apilamiento y la marca terminaria pintada ENCIMA del contenido. Con z-index negativo queda por debajo de todo lo que trae fondo propio (tarjetas, navbar sticky, sidebar del panel) pero por encima del lienzo de la pagina, que es lo que la deja verse en los margenes sin estorbar una sola linea de texto. ESTILO EN public/assets/css/app.css, ni un hex ni un estilo suelto en las vistas: clase .marca-agua (position fixed, centrada con top/left 50% y translate(-50%,-50%), pointer-events none para que no robe un clic, user-select none) y dos tokens nuevos en el :root siguiendo la convencion --ca-*: --ca-marca-agua-opacidad .06 (por encima de .08 el rojo del logo empieza a competir con el texto que tiene encima) y --ca-marca-agua-ancho min(58vw, 360px), que la topa contra la ventana para que en movil no desborde ni saque barra horizontal, y contra 360px para no ampliar mas de la cuenta un PNG de 238x105. Ademas lleva max-width calc(100vw - 2rem) como segundo tope. La imagen es decorativa: alt vacio y aria-hidden para que el lector de pantalla no la anuncie. IMPRESION: .marca-agua entra en la lista de display:none del bloque @media print que ya existia, junto a navbar, sidebar y footer; gastar tinta en un logo al 6% detras del pedido no tiene sentido. NO SE TOCO UNA LINEA DE PHP: sin controladores, sin servicios, sin migraciones y sin tocar la logica; por eso no se corrio pint (no aplica) y la suite se ejecuta solo para comprobar que nada se rompio. Verificado con el servidor en el puerto 8123: /, /consultar, /solicitud, /solicitud/datos, /repuestos/{id} y /admin/login responden 200 y traen exactamente una marca de agua en el HTML, y /logo.png se sirve con content-type image/png. QUEDA FUERA, y se deja dicho: las paginas de error 404/500 son las vistas por defecto del framework, que no cargan app.css; darles la marca exigiria publicar y rediseniar las vistas de errores, que es otra funcionalidad. | developer | V 1.3.3 | 2026-08-20 14:52

2026-08-16 06:40 | ADM-03: parametro inv.actualizar (automatico / manual) y boton "Actualizar" que dispara la sincronizacion de stock a peticion del administrador. EL ID ADM-02 YA ESTABA OCUPADO por el CRUD de parametros, asi que la funcionalidad quedo como ADM-03, el siguiente libre del modulo Administrador. NO SE CREO MIGRACION: es una fila, no una columna, y entra por ParametroSeeder con el mismo criterio que las otras cinco (upsert por col_nombre refrescando SOLO la descripcion, col_sistema = true, valor por defecto 'automatico'); el seeder se corrio contra la base real y sembro 6 parametros. MECANISMO GENERICO DE OPCIONES CERRADAS, no un caso especial escondido en la vista: Parametro::OPCIONES es un mapa nombre => [valor => etiqueta] y Parametro::opcionesDe() es el unico lector; el formulario pinta botones de radio para cualquier parametro que aparezca ahi y el campo de texto de siempre para el resto, y ParametroRequest valida con Rule::in(array_keys(...)) cuando el parametro tiene opciones, de modo que UN VALOR FUERA DE LA LISTA SE RECHAZA EN EL SERVIDOR y no solo en el HTML del radio (probado con 'a_ratos', que vuelve con error de validacion y deja el valor anterior intacto). Se agregaron las constantes Parametro::INV_ACTUALIZAR / INV_AUTOMATICO / INV_MANUAL y no queda ni un literal suelto de esas cadenas en el codigo; el listado imprime la etiqueta ("Automatico") y no la clave, via valor_visible. REFACTOR QUE SOSTIENE TODO LO DEMAS: la orquestacion de la sincronizacion (paginar la API, armar el indice numerico del catalogo, emparejar y escribir en lotes) salio del comando y quedo en el servicio nuevo App\Services\SincronizadorStockRepuestos, que devuelve un App\Services\ResultadoSincronizacionStock (objeto de solo lectura con los contadores: paginas, recibidos, actualizados, sin correspondencia, sin item, item no numerico, sin cantidad, colisiones y no numericos del catalogo, mas las muestras para diagnosticar). El comando repuestos:sincronizar-stock quedo como capa de presentacion en consola (tabla, avisos y --simular) y el controlador del boton llama AL MISMO SERVICIO: consola y panel no pueden divergir. InventarioApiSidocsa no se toco. debeCorrer(), marcarCorrida() y la clave de la ultima corrida se mudaron del comando al servicio y pasaron de estaticas a metodos de instancia, porque son estado operativo del sincronizador y no de la cara en consola; routes/console.php ahora resuelve el servicio del contenedor y las pruebas existentes se adaptaron a esa firma SIN debilitar una sola asercion. EL SCHEDULER RESPETA EL MODO: debeCorrer() exige ahora las dos condiciones, modo automatico Y que hayan pasado los minutos de tiempo.actualizar; en manual la tarea programada NO SE DISPARA NUNCA. EL RESPALDO CUANDO EL PARAMETRO FALTA O ESTA INACTIVO ES 'automatico', Y ES UNA DECISION DELIBERADA: es como se comportaba el sistema antes de que el parametro existiera, y un stock envejeciendo no avisa; si el respaldo fuera 'manual', anular el parametro apagaria la integracion en silencio y nadie se enteraria hasta que el almacen mirara una existencia absurda. Un valor que no este en la lista cerrada (escrito a mano en la base) tambien cae en automatico en vez de dejar el sistema en un modo inexistente. BOTON MANUAL: ruta POST admin/parametros/sincronizar-stock, dentro del grupo con middleware es.admin (el almacenista recibe 403 y NO se manda una sola peticion a la API, verificado con Http::assertNothingSent). El boton se pinta en el listado UNICAMENTE cuando el modo es manual, y que no se pinte no es la barrera: el controlador vuelve a comprobar el modo y responde 409 si esta en automatico, porque adelantar el turno del programador a mano no deja registro de quien lo hizo. Como QUEUE_CONNECTION=sync la sincronizacion corre DENTRO de la peticion, se sube set_time_limit a 600s a proposito y esta explicado en el codigo: paginar el ERP entero se pasa del max_execution_time habitual de 30s y dejar que PHP corte a mitad de la escritura seria peor. El controlador responde JSON cuando expectsJson() y redirige con flash exito/error cuando no, igual que CarritoController::store. CANDADO CONTRA CORRIDAS SIMULTANEAS: el servicio toma Cache::lock('repuestos.sincronizar_stock.candado', 900s) antes de trabajar y si no lo consigue lanza App\Exceptions\SincronizacionEnCursoException, que el controlador traduce a 409 y el comando a un warning con exit code 0 (no es un fallo: otra corrida llego antes). La comprobacion tenia que ser atomica: preguntar por una marca y despues escribirla seria una carrera entre el boton y el programador. Aparte del candado se escribe la marca 'repuestos.sincronizar_stock.en_curso', que es la que LEE EL PANEL para decir "Sincronizacion en curso...", porque un lock no se puede consultar sin intentar adquirirlo y la vista solo quiere mirar; las dos se sueltan en un finally, de modo que un fallo de la API no deja el panel diciendo "en curso" para siempre ni el boton inservible (hay prueba de eso), y las dos vencen a los 900s por si el proceso muere de golpe. NOTIFICACION AL USUARIO EN LOS DOS MODOS, que era parte explicita del pedido. Manual: el formulario lleva data-sincronizar-stock y app.js lo envia por fetch con la misma delegacion de eventos que data-agregar-carrito; mientras corre, el boton queda deshabilitado con spinner y texto "Actualizando..." y sale un toast de aviso al empezar, y al terminar un toast con el resumen (cuantos repuestos actualizados y cuantos codigos sin correspondencia) o uno de error legible; la pagina se recarga 4,2s despues del toast de exito para refrescar la tarjeta de estado sin cortar la lectura del aviso. Automatico: la tarjeta nueva del listado muestra el modo, cada cuantos minutos corre, la ultima corrida y la proxima (calculadas desde la marca del cache y tiempo.actualizar), el recordatorio de que hace falta schedule:work corriendo, y el aviso de corrida en curso cuando la hay. SEGURIDAD: ni codigo_api ni el token pasan por el objeto de resultado, por la respuesta JSON, por el HTML ni por el log. Del catch general solo se repite el mensaje de las RuntimeException que InventarioApiSidocsa construye a proposito (dicen el codigo HTTP y nunca el cuerpo del endpoint de token); cualquier otra excepcion sale como mensaje generico y el detalle queda en el log, porque un error de base de datos o de sistema de archivos delata rutas del servidor. Hay prueba de que un 401 del token responde 502 sin filtrar la credencial. La verificacion del certificado TLS sigue activada y la regla de oro sigue intacta: LA SINCRONIZACION ESCRIBE UNICAMENTE repuestos.stock Y JAMAS cantidad_disponible, ahora sostenida por dos pruebas (la del comando y la nueva del boton manual, que comprueba stock 0 -> 250 con cantidad_disponible clavado en 7). 12 pruebas nuevas (8 en ParametroAdminTest y 4 en SincronizarStockTest), con DatabaseTransactions, Http::fake y codigos numericos de 9 digitos que no chocan con el catalogo real: 78 en total, todas en verde con 229 aserciones, y pint limpio. | developer | V 1.3.2 | 2026-08-16 08:45

2026-08-14 21:05 | CAT-06 (ajuste): se cierran los dos supuestos que habian quedado abiertos en la sincronizacion de stock. EL CAMPO DE CRUCE ES `item`, confirmado por el usuario: InventarioApiSidocsa::CAMPOS_CODIGO (que adivinaba entre id_item/item/codigo/cod_item) se reemplazo por la constante unica CAMPO_CODIGO = 'item' y extraerCodigo() ya no busca variantes; le sigue haciendo trim porque el valor puede venir con espacios alrededor, y si la fila no trae el campo o lo trae vacio devuelve null, se cuenta y se registra un Log::warning con el conteo y el nombre del campo esperado, nunca con el contenido. LA COMPARACION ES NUMERICA, y esa es la parte que importa: repuestos.codigo guarda los codigos con ceros a la izquierda ('0003729') y el ERP manda el mismo item sin ellos ('3729'), asi que se convierten a entero LOS DOS LADOS para emparejar. NO SE HIZO CON UN CAST EN LA CONSULTA a proposito: un `where cast(codigo as int) in (...)` anularia el indice unico de la columna (SQL Server no puede usar un indice cuando la columna va dentro de una funcion) y ademas reventaria con el primer codigo no numerico que entrara a la tabla. En su lugar, como el catalogo tiene apenas 591 filas, SincronizarStockRepuestos::indiceDelCatalogo() las trae enteras a memoria una sola vez y arma un indice intval(codigo) => codigo literal; el emparejamiento ocurre en PHP contra intval(item) y EL UPDATE SIGUE YENDO POR LA COLUMNA codigo CON SU VALOR LITERAL, en lotes de 120 por el limite de 2100 parametros de SQL Server. El entero solo empareja; nunca se escribe ni se consulta por el. TRES CASOS BORDE CUBIERTOS EXPLICITAMENTE. (1) COLISIONES: si dos codigos distintos del catalogo colapsan al mismo entero ('0003729' y '3729', o '00123' y '123'), el indice se pisaria en silencio y el stock terminaria en el repuesto equivocado; ahora se detectan al armarlo, se registran con un Log::warning que nombra los codigos implicados, se avisan en pantalla y QUEDAN LOS DOS FUERA DEL CRUCE en vez de adivinar cual es, porque escribirle la existencia al repuesto equivocado es peor que no escribirla. (2) CODIGOS NO NUMERICOS: se exige ctype_digit y no un intval directo, en los dos lados; intval('ABC') da 0 y habria hecho cruzar ese registro contra cualquier otro que tambien diera 0, que es justo el tipo de dano silencioso que no se descubre hasta que el almacen mira una existencia absurda. Los del catalogo quedan fuera del indice y los de la API se saltan; unos y otros se cuentan aparte y se reportan en la tabla de salida, con muestra de hasta 10 ejemplos. (3) item CON ESPACIOS: se le hace trim antes de convertir, de modo que ' 3729 ' cruza igual que '3729' (verificado: dos filas con el mismo item, una con espacios, cuentan como un solo repuesto y no como dos). SE REVISO LA BASE REAL: sobre los 591 codigos del catalogo NO HAY NINGUNA COLISION Y NINGUN CODIGO NO NUMERICO; todos son de exactamente 7 digitos, asi que el indice cubre hoy el 100% del catalogo y las dos salvaguardas quedan como defensa para lo que entre despues (una carga manual, un CSV importado o un codigo con letras del ERP). Verificacion de punta a punta contra la base real con Http::fake y el primer codigo del catalogo: '0003729' cruzo con el item '3729' y con ' 3729 ', el codigo inexistente se reporto como sin correspondencia y con --simular no se escribio nada. La tabla de salida del comando gano dos filas nuevas (codigos del catalogo no numericos y codigos del catalogo en colision, ambos "fuera del cruce") para que la corrida diga en pantalla cuanto del catalogo quedo sin poder emparejarse. En las pruebas se cambio la que validaba "el codigo se lee de la primera variante presente" por test_el_codigo_se_lee_del_campo_item (que ademas comprueba que id_item y codigo YA NO cuentan) y se agregaron tres casos: el cruce de '00'+numero contra el numero pelado comprobando que el codigo de la tabla no se altera, la colision que deja los dos codigos intactos en su stock anterior, y el item no numerico que se salta sin crear ni mover nada. Los codigos sinteticos de las pruebas pasaron de ser alfanumericos ('T'+uniqid, que con la regla nueva habrian quedado fuera del cruce) a numericos de 9 digitos en el rango 900000000-949999999, mas el relleno de paginacion en 990000000+: el catalogo real usa 7 digitos, asi que no chocan con ningun repuesto de verdad ni colisionan entre si. 66 pruebas en verde y pint limpio. | developer | V 1.3.1 | 2026-08-14 21:40

2026-08-14 19:45 | ADM-02 / CAT-06: modulo de Parametros en el panel y sincronizacion de repuestos.stock contra la API de inventario de Sidocsa. Nueva tabla tbl_parametro (col_nombre unico 100, col_valor 255 NOT NULL default '', col_estado 10 NOT NULL default 'activo', col_descripcion 255 nullable, col_sistema bit default 0) con indice sobre col_estado. EL ESTADO ES TEXTO Y NO BOOLEANO POR PEDIDO EXPLICITO DEL USUARIO, a diferencia del col_activo de tbl_rol y tbl_categoria; para que esa decision no dependa de la disciplina del codigo se agrego el CHECK ck_tbl_parametro_estado ("in ('activo','inactivo')") en la base, que es la ultima linea de defensa cuando entra otro cliente o un script manual, y se verifico rechazando un insert con estado 'apagado'. Las constantes Parametro::ESTADO_ACTIVO / ESTADO_INACTIVO y el scope activos() son el unico sitio donde aparecen esas dos cadenas. Modelo Parametro, ParametroRequest, ParametroAdminController, ParametroService::anular (anular = col_estado 'inactivo', bloqueado para los del sistema, releyendo con lockForUpdate dentro de la transaccion igual que RolService), vistas admin/parametros (listado con filtros, buscador y paginacion; formulario de alta y edicion), entrada en el menu del panel y rutas /admin/parametros protegidas con es.admin. col_sistema no es fillable y ParametroRequest::prepareForValidation le reimpone nombre y estado a los parametros del sistema, porque editar el estado desde el formulario habria sido la puerta trasera para saltarse el bloqueo de anular. ParametroSeeder siembra cinco parametros con col_sistema=true y upsert por col_nombre refrescando SOLO la descripcion, ya que el valor es del administrador y volver a correr el seeder no puede pisar la credencial ni la bodega que configuro: codigo_api=123, tiempo.actualizar=60, api.id_bod=P0XXX, api.id_cia=1 y api.criterio_2='SEGURIDAD '. DOS DE ESOS VALORES SON MARCADORES DE POSICION Y ESTA DICHO EN SU PROPIA DESCRIPCION: codigo_api=123 es un ejemplo que no autentica (se comprobo: la API responde 401 "Codigo invalido") y api.id_bod=P0XXX no es una bodega real; hasta que se corrijan, la sincronizacion no puede funcionar. EL ESPACIO FINAL DE api.criterio_2 ES SIGNIFICATIVO Y SE GUARDA TAL CUAL: no se le hace trim ni en el seeder, ni en Parametro::valor(), ni en ParametroRequest, y se verifico contra la base que la fila mide 10 caracteres y no 9 (nvarchar conserva los espacios finales al almacenar, aunque el operador = de SQL Server los ignore al comparar). AQUI APARECIO UN FALLO REAL QUE ATRAPO LA PRUEBA: el middleware TrimStrings de Laravel recorta toda la entrada de la peticion, asi que guardar el formulario borraba el espacio en silencio y cambiaba la consulta que se le manda a la API sin que nadie lo viera; se corrigio con $middleware->trimStrings(except: ['col_valor']) en bootstrap/app.php, acotado a ese unico campo. Parametro::valor(nombre, porDefecto) es el lector centralizado y devuelve el valor solo si el parametro esta activo; ningun otro punto del codigo consulta la tabla. SINCRONIZACION: nuevo servicio App\Services\InventarioApiSidocsa (token y paginacion contra https://user.appsidocsas.com:98/api/v1) y comando repuestos:sincronizar-stock con --simular, siguiendo el mismo reparto de DetectorColorMarco/repuestos:clasificar (el servicio trata con el endpoint, el comando recorre y escribe). El token vence a los 5 minutos y se cachea 4 con Cache::remember, no 5, para no estrenar uno que expire a mitad de la paginacion; ademas, si una pagina responde 401 se bota el token cacheado y se reintenta UNA sola vez con uno fresco, que es el caso real de una corrida larga que cruza el vencimiento. El cliente HTTP lleva timeout 30s, connectTimeout 10s y reintentos con backoff creciente (500ms, 2s) filtrados con un callback que solo reintenta lo que puede mejorar solo (corte de red, 429 y 5xx): un 401 no se reintenta ahi porque lo resuelve el token nuevo, y un 400 no mejora insistiendo. Se uso retry(..., throw: false) a proposito: con el throw por defecto, Laravel convierte la respuesta 401 en excepcion y el manejo del token vencido nunca se habria ejecutado. La paginacion recorre desde page=1 y corta cuando la pagina viene vacia o trae menos de cant_page (100) registros, siempre con un tope duro de 500 paginas por delante para que un endpoint que responda lleno siempre no deje el comando girando. PUNTO CRITICO QUE QUEDA COMO SUPUESTO: no esta documentado que campo de la respuesta trae el codigo del item para cruzarlo con repuestos.codigo, y no se pudo verificar llamando la API porque codigo_api=123 no autentica; el mapeo esta aislado en InventarioApiSidocsa::extraerCodigo() con la constante CAMPOS_CODIGO = ['id_item','item','codigo','cod_item'], toma la primera variante presente y no vacia, y registra un Log::warning con la cuenta de registros sin campo reconocible. Cuando se confirme el nombre real, esa constante es el UNICO sitio que hay que tocar. Lo mismo se hizo, en menor escala, con la envoltura de la lista (data/datos/resultado/result/items/inventario o lista al desnudo) y con la llave del token (token/access_token/accessToken/Token, tambien dentro de data/datos). La escritura agrupa los codigos por cantidad y hace un UPDATE por valor en lotes de 120 codigos, por el limite de 2100 parametros de SQL Server que ya mordio antes en este proyecto (mismo lote de RepuestoSeeder y repuestos:clasificar); media bodega comparte la misma existencia, empezando por el 0, asi que agrupar convierte miles de filas en pocas sentencias. NO SE CREA NINGUN REPUESTO: los codigos que la API trae y el catalogo no tiene se cuentan, se reportan en pantalla y se registran en el log con una muestra de hasta 10 ejemplos, precisamente para poder diagnosticar de un vistazo si el desfase es de relleno con ceros o de campo equivocado. LA REGLA QUE NO SE PUEDE PERDER: esta sincronizacion escribe UNICAMENTE repuestos.stock y jamas cantidad_disponible. cantidad_disponible es el saldo operativo (el que topa Carrito, el que relee SolicitudService::crearDesdeCarrito dentro de la transaccion y el que descuenta marcarListo con el UPDATE condicionado); si el ERP lo pisara, borraria lo ya despachado y el almacen entregaria contra un saldo fantasma. Esta escrito en la cabecera del comando y sostenido por la prueba test_la_sincronizacion_no_toca_cantidad_disponible. Un fallo de red o de la API no sale como stack trace: se captura, se registra en Log::error y el comando termina con codigo 1 y un mensaje legible (verificado contra el endpoint real: "La API de inventario respondio HTTP 401 al pedir el token", exit code 1). PROGRAMACION en routes/console.php: la tarea se registra con everyMinute() + withoutOverlapping() y un filtro when() que solo la deja pasar cuando ya transcurrieron los minutos de tiempo.actualizar; asi el intervalo real se cambia desde el panel y surte efecto enseguida sin tocar codigo. La marca de la ultima corrida va en el cache (clave repuestos.sincronizar_stock.ultima, timestamp unix) y NO en tbl_parametro, porque es estado operativo y no configuracion: en la tabla apareceria como una fila editable que el administrador no debe tocar, y el cache de este proyecto es la base de datos (CACHE_STORE=database), asi que sobrevive a un reinicio igual que una fila. La marca se pone AL INICIO de la corrida y no al final, para que un fallo de la API tambien espere el intervalo completo en vez de reintentar cada minuto contra un endpoint caido; --simular no la toca. Un tiempo.actualizar ausente o menor a 1 cae a 60 minutos, para que un valor mal escrito no se convierta en una llamada por minuto. SEGURIDAD: ni codigo_api ni el token aparecen en el log, en la salida del comando ni en un mensaje de excepcion (los errores dicen el codigo HTTP, nunca el cuerpo del endpoint de token); Parametro::SENSIBLES marca codigo_api, el listado imprime puntos en vez del valor, el formulario de edicion lo manda vacio (type=password, dejar vacio conserva el actual) y el scope buscar() no busca dentro de col_valor para que no se pueda descubrir una credencial tanteando. LA VERIFICACION DEL CERTIFICADO TLS SE DEJO ACTIVADA Y NO HIZO FALTA TOCARLA: se probo el host real y la conexion a https://user.appsidocsas.com:98 funciona sin advertencias de certificado (responde 401 con JSON, que es lo esperado con la credencial de ejemplo); tambien quedo confirmado que el endpoint de inventario espera el token como Bearer en la cabecera Authorization, que es lo que manda withToken(). 22 pruebas nuevas con Http::fake (ParametroAdminTest 12 y SincronizarStockTest 10: token cacheado y su renovacion, paginacion multi-pagina, 401 que fuerza token nuevo y viaja con el Bearer nuevo, codigos sin correspondencia que no crean repuestos, --simular que no escribe ni consume el turno, la sincronizacion que no toca cantidad_disponible, el fallo de API con exit code distinto de cero, el filtro del programador y el mapeo del codigo); 63 en total, todas en verde, y pint limpio. Migracion aplicada y seeder corrido contra la base real, verificados contra INFORMATION_SCHEMA y sys.check_constraints. | developer | V 1.3.0 | 2026-08-14 20:55

2026-08-13 20:45 | CAT-05: seis columnas nuevas en la tabla repuestos para recibir los datos del ERP que hoy no tienen donde guardarse. Migracion 2026_08_13_100000_add_campos_erp_to_repuestos_table: stock int NOT NULL default 0, abastecimiento tinyint NOT NULL default 0, tamano nvarchar(20) NULL, subgrupo nvarchar(100) NULL, tiene_plano tinyint NOT NULL default 0 y plano_url nvarchar(100) NULL, todas con ->after() para que queden agrupadas detras de stock_minimo. Van SIN prefijo col_ a proposito: repuestos es anterior a la convencion tbl_/col_ y CLAUDE.md pide no mezclar dos estilos dentro de la misma tabla, igual que se hizo con repuestos.categoria_id. LA DECISION DE NULABILIDAD LA IMPONEN LAS 591 FILAS QUE YA EXISTEN: ninguna columna podia entrar NOT NULL sin default, asi que las numericas y las banderas entran NOT NULL con default 0 (el 0 es un valor neutro y ademas verdadero: mientras el ERP no diga otra cosa, no hay existencia conocida ni plano ni abastecimiento) y las tres de texto entran nullable, porque cadena vacia y "todavia no se sabe" no son lo mismo y llenar 591 filas con "" seria inventar un dato que nadie dio; un NULL se puede detectar y completar despues, un "" no se distingue de un tamano que de verdad esta en blanco. SE USO tinyint + CHECK Y NO bit para abastecimiento y tiene_plano: el usuario pidio tinyint con valores 1 y 0, pero tinyint por si solo acepta hasta 255, asi que la restriccion se hizo explicita en la base con dos CHECK (ck_repuestos_abastecimiento y ck_repuestos_tiene_plano, ambos "in (0, 1)") en vez de confiar solo en el cast del modelo; la integridad va en la base, que es la ultima linea de defensa cuando entra otro cliente o un script manual. Por eso el down() suelta los dos CHECK a mano ANTES del dropColumn: SQL Server no deja soltar una columna referenciada por un CHECK y Laravel no los conoce (SqlServerGrammar::compileDropColumn si genera solo el DROP CONSTRAINT de las default constraints, pero de los CHECK no sabe nada), asi que sin ese paso la reversion fallaba. App\Models\Repuesto suma los seis campos a $fillable y los casts stock => integer, abastecimiento => boolean y tiene_plano => boolean, de modo que las banderas se leen y escriben como true/false en PHP y siguen siendo 1/0 en la base. NOTA IMPORTANTE QUE HAY QUE RECORDAR: stock NO reemplaza ni deroga a cantidad_disponible. cantidad_disponible sigue siendo la unica columna que mueve el flujo de pedidos (la que topa App\Services\Carrito, la que relee SolicitudService::crearDesdeCarrito dentro de la transaccion y la que descuenta SolicitudService::marcarListo con el UPDATE condicionado, y la que devuelve SolicitudAdminController::rechazar); stock es el dato informativo que reporta el ERP (la columna Existencia del export para_claude_actual.csv, la misma fuente de la que sale subgrupo) y no lo consume ninguna regla de negocio. Mezclarlas seria el error caro: si el despacho empezara a descontar de stock, la proxima sincronizacion con el ERP pisaria lo despachado. ALCANCE CONTENIDO A PROPOSITO: no se tocaron RepuestoRequest, el formulario de admin/repuestos, ImportarRepuestos ni RepuestoSeeder, porque nada de eso hace falta para que la migracion y el modelo funcionen; los campos nuevos quedan disponibles y sin exponer hasta que se decida quien los llena. Migracion aplicada y verificada contra INFORMATION_SCHEMA sobre las 591 filas: las seis columnas creadas con el tipo, la nulabilidad y el default esperados, y los dos CHECK presentes. | developer | V 1.2.3 | 2026-08-13 21:35

2026-08-12 12:40 | CAT-04: alta en el catalogo de las imagenes de la carga del 05/08 y cierre de los colores de marco que faltaban. Se recorrieron los 252 archivos de public/img con FECHA DE CREACION del 2026-08-05 (el usuario pidio creacion, no modificacion: por LastWriteTime son 269, y los 17 de diferencia se dejaron fuera a proposito). El codigo se deriva del prefijo numerico del nombre de archivo y repuestos.foto guarda el nombre completo, igual que hace ImportarRepuestos::buscarFoto. Entraron 102 repuestos (489 -> 591) con upsert por codigo en lotes de 120 dentro de una transaccion, por el limite de 2100 parametros de SQL Server; los campos que no vienen de la imagen salen del mismo criterio determinista crc32 del seeder, asi que las filas nuevas son indistinguibles de las 489 originales. EXCLUIDOS: 3 archivos sin codigo derivable (activo_fijo_*), 5 duplicados que derivaban al mismo codigo con distinta extension (gana el primero alfabeticamente) y 4 codigos en conflicto de asignacion; sobre estos ultimos se amplio el criterio recibido: 0016559.jpg y 0016959.jpg resultaron ser EL MISMO BINARIO (MD5 F24292AFE261), asi que se excluyo el CODIGO y no solo el nombre de archivo, porque elegir uno de los dos habria sido decidir por el almacen a que item pertenece la foto. Ninguno de los 138 codigos que ya existian necesito actualizar su foto. CUATRO CATEGORIAS NUEVAS en CategoriaSeeder (upsert por col_slug refrescando solo col_color_hex, para no pisar el nombre que ponga el administrador): Rosa #D890D8 (33), Gris #A8A8A8 (13), Fucsia #A80078 (8) y Amarillo #F0F000 (3). DECISION: el rosa venia en dos tonos y se separo en DOS categorias, no una. Comparten el tramo de matiz >= 290 pero se verifico en las fotos que son dos marcos distintos del almacen (0003961.jpg rosa pastel contra 0003507.jpg fucsia intenso), no la misma tinta con mas o menos carga; los separa la luminosidad (180 contra 84) con el corte en 132, el punto medio, mismo criterio que ya separa el durazno del naranja. NO se creo la categoria "blanco" que se habia pedido: su unico caso, 0040469_1785262131257.jpeg, resulto ser una foto de celular de un motor embalado en la bodega, sin marco, y el detector estaba midiendo el piso con apenas 9.3% de dominancia. El mismo falso positivo tenian 0040470 (negro, 12.9%) y 0040471 (celeste, 14.2%), tambien fotos de celular: se les puso categoria_id en null. NO se puso un umbral minimo de dominancia para atajar ese caso porque no separa: la foto legitima 0031454.jpg tiene 13.3%, por debajo de los falsos positivos, y el umbral habria movido un repuesto real de categoria. Tras tocar DetectorColorMarco se corrio repuestos:clasificar --simular --forzar y las siete categorias originales quedaron identicas (Rojo 153, Celeste 104, Morado 94, Negro 57, Azul 48, Verde 42, Durazno 31), asi que la calibracion contra las fotos viejas sigue intacta. REFACTOR: RepuestoSeeder quedo consumiendo App\Services\GeneradorDatosRepuesto y se elimino la duplicacion inline; la equivalencia se comprobo archivo por archivo sobre los 758 de public/img con cero diferencias, porque cualquier cambio ahi habria alterado los repuestos ya publicados al volver a sembrar. Reparto final: Rojo 153, Celeste 103, Morado 94, Negro 56, Azul 48, Verde 42, Rosa 33, Durazno 31, Gris 13, Fucsia 8, Amarillo 3; 7 sin categoria (4 PNG cuyo marco no se distingue del margen y las 3 fotos de celular). Las 591 fotos tienen archivo fisico en public/img. Cuatro casos nuevos en DetectorColorMarcoTest y una prueba de que el rosa y el fucsia no se confunden: 41 pruebas en verde y pint limpio. | developer | V 1.2.2 | 2026-08-12 13:40

2026-08-05 12:40 | CAT-03 (parcial): sincronizacion de fotos desde el storage de Supabase. Nuevo comando repuestos:sincronizar-supabase (--prefijo, --csv, --forzar, --simular) que lista el bucket paginando de a 100 con el endpoint object/list, entra recursivamente en las subcarpetas (una fila con id nulo es carpeta, no archivo) y baja a public/img solo lo que falta. La descarga usa object/public/... cuando SUPABASE_BUCKET_PUBLICO es true y object/... con Bearer cuando es false; el listado en cambio SIEMPRE manda la clave, porque pasa por RLS aunque el bucket sea publico. Configuracion en config/services.php bloque supabase y cuatro variables en .env (SUPABASE_URL, SUPABASE_BUCKET, SUPABASE_KEY, SUPABASE_BUCKET_PUBLICO); la clave nunca se versiona. Se decidio bajar los archivos a disco y NO servirlos desde Supabase porque DetectorColorMarco necesita el archivo local para leer el color del marco con GD: mover las fotos a una URL remota habria roto repuestos:clasificar. public/img paso de 489 a 895 archivos: 894 imagenes listadas, 406 descargadas, 488 omitidas por existir ya, cero fallidas. Como public/img es plano y el bucket tiene carpetas, el comando avisa y omite cuando dos rutas traen el mismo basename en vez de pisar en silencio; en esta corrida no hubo ninguna colision. HALLAZGO IMPORTANTE: la carpeta documentos/ del bucket trae para_claude_actual.csv, el export del ERP con 29.127 items unicos (Item, Desc. item, U.M., Existencia, GRUPO, SUBGRUPO, ubicacion de control), y los 595 codigos que hoy tienen foto estan TODOS ahi, asi que los nombres reales no hay que inventarlos. Quedan 106 codigos con foto que aun no existen en la tabla repuestos. DOS COSAS PENDIENTES ANTES DE IMPORTAR: (1) 150 de los archivos nuevos son fotos alternas con nombres que el catalogo no reconoce (0001220 (1).jpg, 0016559 - copia.jpg, 0036475_1785790062456.png) y hay que renombrarlas a {codigo}_v2.ext o descartarlas, porque buscarFoto() solo entiende {codigo} y {codigo}_v2; (2) 8 archivos traen la extension cambiada respecto de su contenido real (PNG guardados como .jpg y viceversa) y DetectorColorMarco abre por extension, asi que fallarian al clasificar. No se creo ningun repuesto ni se toco la base de datos en este paso. | developer | V 1.2.1 | 2026-08-05 13:31

2026-08-04 19:30 | CAT-01 / CAT-02: modulo CRUD de Categorias y clasificacion automatica del catalogo por el color del marco de la foto. HALLAZGO QUE ORIENTA TODO: las 489 imagenes de public/img llevan un marco de color en el borde y ESE color es la categoria del almacen; los nombres no correlacionan (cada color mezcla filtros, valvulas, tornillos y rodamientos), asi que inferir la categoria por el nombre no era viable. Nueva tabla tbl_categoria (col_nombre, col_slug unico, col_color_hex, col_descripcion, col_activo) y columna repuestos.categoria_id nullable con FK nullOnDelete e indice; nullable a proposito para que un repuesto sin foto reconocible no bloquee nada. Nuevo servicio DetectorColorMarco (muestrea la banda exterior del 8%, descarta el casi-blanco del margen, cuantiza a bloques de 24, clasifica por saturacion/matiz/luminosidad) y comando repuestos:clasificar con --forzar y --simular, que escribe en lotes de 120 ids por el limite de 2100 parametros de SQL Server y aborta con mensaje claro si falta la extension GD. Las 489 fotos clasificaron sin una sola excepcion: Rojo 153, Celeste 102, Morado 83, Negro 56, Verde 42, Durazno 30, Azul 23; cero sin clasificar. DOS DECISIONES QUE VALE LA PENA RECORDAR: (1) azul marino y celeste son categorias distintas y no se fusionan, las separa el corte de matiz en 200 grados; (2) el durazno #F0C0A8 cae en matiz 20 igual que un naranja fuerte y lo que los distingue es la luminosidad alta (>=180). La relacion en Repuesto se llama categoriaAsignada() y no categoria() porque la tabla YA tenia una columna de texto categoria (la linea generica del seeder) que la habria opacado, el mismo caso documentado de User::rolAsignado(); esa columna se dejo intacta y en el catalogo publico quedo etiquetada "Tipo de repuesto" para que dos filtros no se llamen igual. col_slug es inmutable porque es la clave con la que el detector y el seeder reconocen el registro: no es fillable, lo deriva CategoriaService::crear() del nombre y update no lo toca, de modo que renombrar la categoria desde el panel es seguro. CategoriaService::anular() releé con lockForUpdate() dentro de la transaccion y bloquea la anulacion si hay repuestos activos asociados; anular pone col_activo=false y no borra, porque los repuestos apuntan por FK. CRUD en /admin/categorias protegido con es.admin, entrada en el menu del panel, filtro por categoria en el catalogo publico con data-autoenviar y categoria visible en la vista de detalle. El chip de color lleva el hex en linea: es la unica excepcion legitima a "no hex sueltos en las vistas" porque col_color_hex es contenido (el color real del marco), no una decision de diseno; la forma vive en .chip-color de app.css. 25 pruebas nuevas (CategoriaAdminTest con 16 y DetectorColorMarcoTest con 9, que incluye una foto real por cada uno de los siete colores); 36 en total, todas en verde, y pint limpio. | developer | V 1.2.0 | 2026-08-04 20:55

2026-08-04 18:30 | UI-01: cambio de paleta a rojo de senalizacion #f20a0a con fondo #fafafa. Se sustituyo la paleta teal + slate azulado por una sola familia de neutros calidos (zinc de estanteria) derivada de #fafafa, para que el rojo no quede peleando con grises frios: --ca-borde, --ca-texto-*, sombras, panel lateral (grafito #1c1a18 en vez del azul marino #0f172a), hovers de tarjeta y metrica, linea de tiempo, cinta de agotado e impresion. REGLA DE LOS DOS ROJOS: la marca #f20a0a va siempre en relleno solido (accion principal, filete de "estas aqui", indicadores) y lo destructivo va en oxido #8f1616 siempre contorneado o en fondo tenue; lo que separa marca de peligro es la FORMA, no el tono. Ningun estado de Solicitud usa el rojo de marca. Contrastes verificados: #f20a0a 4.35:1 sobre blanco (relleno, indicador y texto grande, no texto pequeno) y #c40808 6.20:1 para texto de marca y enlaces. Se corrigieron tres fallos de contraste heredados: --ca-neutro-500 (.text-muted pasaba de 3.66:1 a 4.77:1), placeholder de los inputs (2.28:1 a 4.57:1) y el contorno del boton de marca (1.48:1 a 3.24:1); el boton destructivo ahora lleva contorno pleno. El contador del carrito dejo de ser text-bg-danger (decia "error") y paso a chip grafito. Las dos plantillas de correo se reescribieron con los colores en linea, ya que no cargan app.css. welcome.blade.php se dejo intacta: es la plantilla por defecto de Laravel y ninguna ruta la usa. Cambio solo visual: no se toco logica PHP, controladores, servicios ni migraciones; las 11 pruebas siguen en verde. | developer | V 1.1.1 | 2026-08-04 19:16

2026-08-01 19:45 | ADM-01: modulo CRUD de Roles en el panel. Nueva tabla tbl_rol (col_clave, col_nombre, col_descripcion, col_gestiona_catalogo, col_sistema, col_activo) y columna users.rol_id con llave foranea, rellenada desde el texto historico de users.rol; ambas conviven (paso 1 del cambio compatible). Modelo Rol, RolRequest, RolAdminController y RolService::anular (anular = col_activo false, bloqueado para roles del sistema y para roles con usuarios activos). Vistas admin/roles (listado con filtros, buscador y paginacion; formulario de alta y edicion), entrada en el menu del panel y rutas admin/roles protegidas con el middleware es.admin. User::puedeGestionarCatalogo ahora obedece al rol asignado y cae a users.rol cuando no hay rol_id. UsuarioSeeder escribe rol y rol_id. Nueva prueba tests/Feature/RolAdminTest (9 casos, todos en verde). | developer | V 1.1.0 | 2026-08-01 20:05
